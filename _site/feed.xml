<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" ><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="http://localhost:4000/feed.xml" rel="self" type="application/atom+xml" /><link href="http://localhost:4000/" rel="alternate" type="text/html" /><updated>2026-03-20T13:35:50+00:00</updated><id>http://localhost:4000/feed.xml</id><title type="html">Louis Meyer</title><subtitle>This is my online attic</subtitle><entry><title type="html">Dependency and Discrepancy Measures in RKHS</title><link href="http://localhost:4000/blog/Kernel/" rel="alternate" type="text/html" title="Dependency and Discrepancy Measures in RKHS" /><published>2000-01-06T00:00:00+00:00</published><updated>2000-01-06T00:00:00+00:00</updated><id>http://localhost:4000/blog/Kernel</id><content type="html" xml:base="http://localhost:4000/blog/Kernel/"><![CDATA[<p><em>This blog post provides an introduction to kernel methods in modern statistical learning, starting from the foundational theory of reproducing kernel Hilbert spaces (RKHS), then developing on the theory of mean embeddings and covariance operators. Three major kernel-based hypothesis testing frameworks: Maximum Mean Discrepancy, the Hilbert-Schmidt Independence Criterion, and the Kernel Stein Discrepancy are introduced.</em></p>

<h2 id="1-primer-on-kernels">1. Primer on Kernels</h2>

<h3 id="11-rkhs-and-properties-of-kernels">1.1 RKHS and Properties of Kernels</h3>

<p>This section aims to be an introduction to kernel functions, reproducing kernel Hilbert spaces (RKHS), and their main theorems and properties, which are direct results of functional analysis. It is intended to be introductory and to present these concepts from a theoretical standpoint. Subsequent sections will emphasize applied and practical aspects of kernel methods in statistical frameworks. For a more complete and rigorous treatment, we refer to Smola &amp; Schölkopf (1998) and Berlinet &amp; Thomas-Agnan (2011).</p>

<h4 id="111-kernel">1.1.1 Kernel</h4>

<p><strong>Definition (Kernel).</strong> A function $k : \mathcal{X} \times \mathcal{X} \rightarrow \mathbb{R}$ is a <em>kernel</em> if it is symmetric and positive semi-definite: for any $n \in \mathbb{N}$, points ${x_1, \ldots, x_n} \subset \mathcal{X}$, and coefficients $c_1, \ldots, c_n \in \mathbb{R}$,</p>

\[\sum_{i=1}^{n} \sum_{j=1}^{n} c_i c_j k(x_i, x_j) \geq 0.\]

<p>Equivalently, the Gram matrix $\mathbf{K} \in \mathbb{R}^{n \times n}$ with $K_{ij} = k(x_i, x_j)$ is positive semi-definite for any finite point set.</p>

<p><strong>Remark.</strong> Every kernel admits a feature map representation $k(x, x’) = \langle \phi(x), \phi(x’) \rangle_{\mathcal{H}}$ for some Hilbert space $\mathcal{H}$, which will be formalized via the associated RKHS.</p>

<p><strong>Example.</strong> Standard kernels on $\mathbb{R}^d$ include the linear kernel $k(x, x’) = x^\top x’$, the polynomial kernel $k(x, x’) = (x^\top x’ + c)^p$, the RBF kernel $k(x, x’) = \exp(-|x - x’|^2/2\sigma^2)$, and the Laplacian kernel $k(x, x’) = \exp(-|x - x’|/\sigma)$.</p>

<p><strong>Proposition (Closure and composition properties).</strong> Kernels are closed under positive linear combinations ($\alpha k_1 + \beta k_2$ for $\alpha, \beta \geq 0$), products ($k_1 \cdot k_2$), pointwise limits, and tensor products. Moreover, if $k$ is a kernel on $\mathcal{X}$ and $\psi: \mathcal{Y} \rightarrow \mathcal{X}$, then $k(\psi(\cdot), \psi(\cdot))$ is a kernel on $\mathcal{Y}$. Products preserve universality and characteristic properties.</p>

<h4 id="112-rkhs">1.1.2 RKHS</h4>

<p><strong>Definition (Hilbert Space).</strong> A <em>Hilbert space</em> $\mathcal{H}$ is a complete inner product space, i.e., a vector space equipped with an inner product $\langle \cdot, \cdot \rangle_{\mathcal{H}}$ such that every Cauchy sequence converges in the induced norm \(\|f\|_{\mathcal{H}} = \sqrt{\langle f, f \rangle_{\mathcal{H}}}\).</p>

<p><strong>Definition (Reproducing Kernel Hilbert Space).</strong> A Hilbert space $\mathcal{H}$ of real-valued functions $f: \mathcal{X} \rightarrow \mathbb{R}$ is a <em>reproducing kernel Hilbert space</em> (RKHS) if point evaluation functionals are continuous. That is, for each $x \in \mathcal{X}$, the evaluation functional $\delta_x: \mathcal{H} \rightarrow \mathbb{R}$ defined by $\delta_x(f) = f(x)$ is bounded.</p>

<p><strong>Theorem (Riesz Representation).</strong> Let $\mathcal{H}$ be a Hilbert space and $L: \mathcal{H} \rightarrow \mathbb{R}$ a bounded linear operator. Then there exists a unique element $h \in \mathcal{H}$ such that $Lf = \langle f, h \rangle_{\mathcal{H}}$ for all $f \in \mathcal{H}$.</p>

<p><strong>Proposition (Reproducing kernel).</strong> By the Riesz representation theorem, for each $x \in \mathcal{X}$, there exists a unique element $k(\cdot, x) \in \mathcal{H}$ such that</p>

\[f(x) = \langle f, k(\cdot, x) \rangle_{\mathcal{H}} \quad \text{for all } f \in \mathcal{H}.\]

<p>This is the <em>reproducing property</em>. The function $k: \mathcal{X} \times \mathcal{X} \rightarrow \mathbb{R}$ defined by $k(x, x’) = \langle k(\cdot, x), k(\cdot, x’) \rangle_{\mathcal{H}}$ is called the <em>reproducing kernel</em> of $\mathcal{H}$. The kernel $k$ is uniquely determined by $\mathcal{H}$, and conversely, $\mathcal{H}$ is uniquely determined by $k$.</p>

<p><strong>Theorem (Moore-Aronszajn).</strong> There is a one-to-one correspondence between kernels on $\mathcal{X}$ and RKHSs of real-valued functions on $\mathcal{X}$. Given any kernel $k$, there exists a unique RKHS $\mathcal{H}_k$ with reproducing kernel $k$. Conversely, every RKHS uniquely determines its reproducing kernel.</p>

<p><strong>Remark (The RKHS norm).</strong> The RKHS norm can be interpreted through the reproducing property. For \(f \in \mathcal{H}_k\), we can write $f$ as a (possibly infinite) linear combination of kernel evaluations. The norm $|f|_{\mathcal{H}_k}$ measures the complexity or smoothness of $f$ in a way that depends on the choice of kernel $k$. Functions that align with the kernel structure have smaller norms.</p>

<p><strong>Definition (Kernel integral operator).</strong> Let $\mu$ be a measure on $\mathcal{X}$. The <em>kernel integral operator</em> $T_k: L^2(\mathcal{X}, \mu) \rightarrow L^2(\mathcal{X}, \mu)$ associated with kernel $k$ is defined by</p>

\[(T_k f)(x) = \int_{\mathcal{X}} k(x, x') f(x') \, d\mu(x').\]

<p>Under appropriate conditions, $T_k$ is a compact, self-adjoint, positive operator.</p>

<p><strong>Theorem (Mercer).</strong> Let $\mathcal{X} \subset \mathbb{R}^d$ be compact, $\mu$ a finite Borel measure with support $\mathcal{X}$, and $k: \mathcal{X} \times \mathcal{X} \to \mathbb{R}$ a continuous positive definite kernel. Then the integral operator $T_k$ on $L^2(\mathcal{X}, \mu)$ is compact, self-adjoint, and positive, with a countable set of eigenvalues \(\{\lambda_i\}_{i=1}^{\infty}\) (with $\lambda_i \geq 0$, $\lambda_i \to 0$) and corresponding orthonormal eigenfunctions ${\phi_i}_{i=1}^{\infty}$ forming an orthonormal basis of $L^2(\mathcal{X}, \mu)$. The kernel admits the absolutely and uniformly convergent representation</p>

\[k(x, x') = \sum_{i=1}^{\infty} \lambda_i \phi_i(x) \phi_i(x'), \quad \forall x, x' \in \mathcal{X}.\]

<p>The RKHS \(\mathcal{H}_k\) consists of all functions $f \in L^2(\mathcal{X}, \mu)$ with expansion $f = \sum_{i=1}^{\infty} \alpha_i \phi_i$ satisfying</p>

\[\|f\|_{\mathcal{H}_k}^2 = \sum_{i=1}^{\infty} \frac{\alpha_i^2}{\lambda_i} &lt; \infty,\]

<p>where $\alpha_i = \langle f, \phi_i \rangle_{L^2(\mathcal{X}, \mu)}$. This norm follows from the orthogonal expansion and the identity \(\langle \phi_i, \phi_j \rangle_{\mathcal{H}_k} = \delta_{ij}/\lambda_i\).</p>

<p>Note that any function in the RKHS $\mathcal{H}$ can be written as a linear combination of the eigenfunctions, via Mercer’s theorem, i.e. $f = \sum_{i=1}^{\infty} \alpha_i \phi_i(x) \quad \forall f \in \mathcal{H}_k$.</p>

<p><strong>Remark (Isometric views of the RKHS).</strong> Mercer’s theorem reveals three equivalent perspectives on the RKHS:</p>

<ol>
  <li>The <em>function space view</em>: $\mathcal{H}_k$ as a space of functions with the reproducing property</li>
  <li>The <em>feature space view</em>: Functions as elements in a potentially infinite-dimensional Hilbert space with feature map $\phi: x \mapsto (\sqrt{\lambda_i}\phi_i(x))_{i=1}^{\infty}$, where $k(x, x’) = \langle \phi(x), \phi(x’) \rangle$</li>
  <li>The <em>sequence space view</em>: $\mathcal{H}_k$ is isometrically isomorphic to the weighted $\ell^2$ space $\mathcal{H}_k \cong \ell^2(\lambda) = {(\alpha_i): \sum_i \alpha_i^2/\lambda_i &lt; \infty}$</li>
</ol>

<p><strong>Remark (The kernel trick).</strong> In practice, kernels provide a powerful analytical tool for performing comparisons through inner products in potentially infinite-dimensional feature spaces, without ever having to explicitly augment the data or compute the feature map $\phi$. Given data points $x_1, \ldots, x_n \in \mathcal{X}$, we can compute inner products in the feature space simply by evaluating $k(x_i, x_j)$. This “kernel trick” allows algorithms that depend only on inner products (such as ridge regression, SVMs, and PCA) to be kernelized, operating implicitly in high- or infinite-dimensional spaces while maintaining computational tractability. The choice of kernel thus encodes prior knowledge about what constitutes similarity in the problem domain.</p>

<p><strong>Proposition (Derivative reproducing).</strong> Let $k$ be a smooth kernel such that all partial derivatives $\partial^{\alpha} k(x, x’)$ exist and are continuous. If $f \in \mathcal{H}_k$ is sufficiently smooth, then the derivatives of $f$ can be computed via</p>

\[\partial^{\alpha} f(x) = \langle f, \partial^{\alpha}_x k(\cdot, x) \rangle_{\mathcal{H}_k},\]

<p>where the derivative is taken with respect to the first argument of $k$. This extends the reproducing property to derivatives, which is particularly useful in operator learning and solving differential equations in RKHS.</p>

<h3 id="12-mean-embedding">1.2 Mean Embedding</h3>

<p><strong>Definition (Kernel Mean Embedding).</strong> Given a kernel $k$ with reproducing kernel Hilbert space (RKHS) \(\mathcal{H}_k\), the kernel mean embedding of a probability measure \(\mathbb{P} \in \mathcal{M}^1_+(\mathcal{X})\) is the element \(\mu_\mathbb{P} \in \mathcal{H}_k\) defined by</p>

\[\mu_\mathbb{P} = \mathbb{E}_{X \sim \mathbb{P}}[k(X, \cdot)] = \int_{\mathcal{X}} k(x, \cdot) \, \mathrm{d}\mathbb{P}(x)\]

<p>Using the reproducing property of \(\mathcal{H}_k\), \(\mu_\mathbb{P}\) is the unique element of \(\mathcal{H}_k\) satisfying \(\langle f, \mu_\mathbb{P} \rangle_{\mathcal{H}_k} = \mathbb{E}_{X \sim \mathbb{P}}[f(X)] \quad \forall f \in \mathcal{H}_k\).</p>

<p>In particular, for $f = k(\cdot, x)$, this yields \(\mu_\mathbb{P}(x) = \langle \mu_\mathbb{P}, k(\cdot, x) \rangle_{\mathcal{H}_k} = \mathbb{E}_{X \sim \mathbb{P}}[k(X, x)]\).</p>

<p>Given an i.i.d. sample ${x_1,\dots,x_m}$ from $\mathbb{P}$, the empirical kernel mean embedding is</p>

\[\hat{\mu}_\mathbb{P} = \frac{1}{m} \sum_{i=1}^m k(x_i, \cdot)\]

<!-- ![Diagram](/images/KMEx_light.png) -->

<p align="center">
<img src="/images/KMEx_light.png" data-light-src="/images/KMEx_light.png" data-dark-src="/images/KMEx_dark.png" alt="Diagram" />
</p>

<p><strong>Remark (Existence).</strong> The embedding \(\mu_\mathbb{P}\) exists in \(\mathcal{H}_k\) when \(\mathbb{E}_{X \sim \mathbb{P}}[\sqrt{k(X,X)}] &lt; \infty\), which holds automatically for bounded kernels.</p>

<p><strong>Definition (Characteristic Kernel).</strong> A kernel $k$ is <em>characteristic</em> if the mean embedding map $\mathbb{P} \mapsto \mu_\mathbb{P}$ is injective, meaning $\mu_\mathbb{P} = \mu_\mathbb{Q}$ if and only if $\mathbb{P} = \mathbb{Q}$.</p>

<h3 id="13-tensor-product-space-and-covariance-operators">1.3 Tensor Product Space and Covariance Operators</h3>

<h4 id="131-tensor-product-space">1.3.1 Tensor Product Space</h4>

<p>Let $(\mathcal{H}, k)$ and $(\mathcal{G}, \ell)$ be two reproducing kernel Hilbert spaces over input spaces $\mathcal{X}$ and $\mathcal{Y}$ respectively. The tensor product space $\mathcal{G} \otimes \mathcal{H}$ provides a natural framework for studying joint distributions and dependence structures between random variables taking values in $\mathcal{X}$ and $\mathcal{Y}$.</p>

<p><strong>Definition (Tensor Product RKHS).</strong> The tensor product space $\mathcal{G} \otimes \mathcal{H}$ is the completion of the linear span of elementary tensors ${g \otimes h : g \in \mathcal{G}, h \in \mathcal{H}}$ under the inner product</p>

\[\langle g_1 \otimes h_1, g_2 \otimes h_2 \rangle_{\mathcal{G} \otimes \mathcal{H}} = \langle g_1, g_2 \rangle_{\mathcal{G}} \langle h_1, h_2 \rangle_{\mathcal{H}}.\]

<p>The space $\mathcal{G} \otimes \mathcal{H}$ is itself an RKHS over $\mathcal{Y} \times \mathcal{X}$ with reproducing kernel $k \otimes \ell$:</p>

\[(k \otimes \ell)((y,x), (y',x')) = k(x,x') \ell(y,y').\]

<p>Any functional tensor $g \otimes h$ can be interpreted as a linear operator from $\mathcal{H}$ to $\mathcal{G}$ via the mapping</p>

\[(g \otimes h)(f) = \langle h, f \rangle_{\mathcal{H}} g \quad \text{for } f \in \mathcal{H}.\]

<p>This operator takes an element $f \in \mathcal{H}$, computes its inner product with $h$ in the RKHS $\mathcal{H}$ (yielding a scalar), and scales the element $g \in \mathcal{G}$ by this scalar. Using the <em>reproducing property</em> with feature maps $\phi(y) = \ell(y, \cdot) \in \mathcal{G}$ and $\Phi(x) = k(x, \cdot) \in \mathcal{H}$, we have</p>

\[(\phi(y) \otimes \Phi(x))(f) = \langle \Phi(x), f \rangle_{\mathcal{H}} \phi(y) = f(x) \phi(y).\]

<p>For any $g, f \in \mathcal{G}$ and $u, v \in \mathcal{H}$,</p>

\[\langle g, (u \otimes v)(f) \rangle_{\mathcal{G}} = \langle g, \langle v, f \rangle_{\mathcal{H}} u \rangle_{\mathcal{G}} = \langle v, f \rangle_{\mathcal{H}} \langle g, u \rangle_{\mathcal{G}} = \langle u \otimes v, g \otimes f \rangle_{\mathcal{G} \otimes \mathcal{H}}.\]

<p>This identity shows that evaluating the operator $u \otimes v$ on $f$ and taking the inner product with $g$ in $\mathcal{G}$ is equivalent to computing the tensor product inner product of $u \otimes v$ with $g \otimes f$ in $\mathcal{G} \otimes \mathcal{H}$.</p>

<p><strong>Definition (Hilbert-Schmidt Operators).</strong> Let the bounded linear operator \(C: \mathcal{H} \to \mathcal{G}\) on separable Hilbert Spaces \(\mathcal{H}\) and \(\mathcal{G}\) with orthonormal basis \((h_i)_{i \in I}\) and \((g_j)_{j \in J}\) respectively. Define the Hilbert–Schmidt norm of $C$ by</p>

\[\begin{align*}
\|C\|_{\mathrm{HS}}^{2} &amp;= \sum_{i \in I} \|C h_i\|_{\mathcal{G}}^{2} \\
&amp;= \sum_{i \in I} \sum_{j \in J} \big| \langle C h_i , g_j \rangle_{\mathcal{G}} \big|^{2}.
\end{align*}\]

<p>The Hilbert-Schmidt norm \(\|\cdot\|_{\text{HS}}\) is independent of the choice of orthonormal basis \(I, J\), which are assumed to be either finite or countably infinite. An operator $C$ is called a <em>Hilbert–Schmidt operator</em> if \(\|C\|_{\mathrm{HS}} &lt; \infty\).</p>

<h4 id="132-covariance-operators">1.3.2 Covariance Operators</h4>

<p>We now introduce the concepts of Cross-Covariance operator on reproducing kernel Hilbert spaces, as well as their normalized and conditional versions. The Cross-Covariance operator is a generalization of the covariance matrix to infinite dimensional feature spaces, used to capture dependence (and conditional dependence) of random variables under non-linear feature maps. The theoretical foundations and practical applications of Cross-Covariance operators have been introduced and thoroughly studied by K. Fukumizu, and we refer to his work for further details: Fukumizu et al. (2004, 2007, 2009). Most of the following definitions can be viewed as equivalent formulations of covariance, correlations and conditional correlations measures in Euclidean spaces generalized through non-linear kernels in reproducing kernel Hilbert spaces.</p>

<p>Let \(\mathcal{H}_\mathcal{X}\) and \(\mathcal{H}_\mathcal{Y}\) be two RKHS of functions on Borel sets $\mathcal{X}$ and $\mathcal{Y}$, with reproducing kernels $k_\mathcal{X}$ and $k_\mathcal{Y}$, respectively. Assuming integrability, i.e.</p>

\[\mathbb{E}[k_\mathcal{X}(X,X)]&lt; \infty \quad\forall X \in \mathcal{X}, \quad \text{and} \quad \mathbb{E}[k_\mathcal{Y}(Y,Y)]&lt; \infty \quad\forall Y \in \mathcal{Y}\]

<p>we can define the cross-covariance operator of $(X,Y)$:</p>

<p><strong>Definition (Cross-Covariance Operator).</strong> The <em>centred cross-covariance operator</em> of $(X,Y)$, denoted as $\Sigma_{YX}$, is an operator from \(\mathcal{H}_\mathcal{X}\) to \(\mathcal{H}_\mathcal{Y}\) defined, for all \(f \in \mathcal{H}_\mathcal{X}\) and \(g \in \mathcal{H}_\mathcal{Y}\), by:</p>

\[\begin{align*}
\langle g, \Sigma_{YX}f\rangle_{\mathcal{H}_\mathcal{Y}} &amp;= \text{Cov}[f(X), g(Y)] \\
&amp;= \mathbb{E}_{YX}[(f(X) - \mathbb{E}_{X \sim \mathbb{P}_X}[f(X)]) \cdot (g(Y) - \mathbb{E}_{Y \sim \mathbb{P}_Y}[g(Y)])] \\
&amp;= \mathbb{E}_{YX}[\langle f, k_\mathcal{X}(X, \cdot) - \mu_{\mathbb{P}_X}\rangle \langle g, k_\mathcal{Y}(Y, \cdot) - \mu_{\mathbb{P}_Y}\rangle ] \\
&amp;= \mathbb{E}_{YX}[\langle f \otimes g, (k_\mathcal{X}(X, \cdot) - \mu_{\mathbb{P}_X}) \otimes (k_\mathcal{Y}(Y, \cdot) - \mu_{\mathbb{P}_Y})\rangle ]
\end{align*}\]

<p>Using the <em>Riesz Representation Theorem</em>, one can also write:</p>

\[\Sigma_{YX} = \mathbb{E}_{YX}[(k_\mathcal{X}(X, \cdot) - \mu_{\mathbb{P}_X}) \otimes (k_\mathcal{Y}(Y, \cdot) - \mu_{\mathbb{P}_Y})]\]

<p>The assumptions of integrability implies that the <em>cross-covariance operator</em> is Hilbert Schmidt. The operator represents higher-order correlations of $X$ and $Y$ through feature maps $f(X)$ and $g(Y)$. Analogously to the variance in Euclidean spaces, if $X = Y$, $\Sigma_{XX}$ is called the <em>covariance operator</em>. The <em>cross-covariance operator</em> can be used to determine the independence of $X$ and $Y$: $\Sigma_{YX} = 0 \iff X$ and $Y$ are independent.</p>

<p><strong>Definition (Normalized Cross-Covariance Operator).</strong> The <em>normalized cross-covariance operator</em> of $(X,Y)$, denoted as $V_{YX}$, is a unique bounded operator such that:</p>

\[\Sigma_{YX} = \Sigma_{YY}^{1/2}V_{YX}\Sigma_{XX}^{1/2}\]

<p>The <em>normalized cross-covariance operator</em> can be seen as an analogue to the correlation, and similarly, its Hilbert Schmidt norm is less than or equal than 1.</p>

<p>Finally, if we consider another random variable $Z$ on \(\mathcal{Z}\), and the RKHS \(\mathcal{H}_{\mathcal{Z}}\) with corresponding kernel \(k_\mathcal{Z}\), we can define the <em>normalized conditional cross-covariance operator</em>.</p>

<p><strong>Definition (Normalized Conditional Cross-Covariance Operator).</strong> The <em>normalized conditional cross-covariance operator</em> of $X$ and $Y$ given $Z$, denoted as \(V_{YX\|Z}\), is defined as:</p>

\[V_{YX|Z} = V_{YX} - V_{YZ}V_{XZ}\]

<p>Alternatively, it can also be defined as:</p>

\[V_{YX|Z} = \Sigma_{YY}^{-1/2} \Sigma_{YX|Z} \Sigma_{XX}^{-1/2} \qquad \text{with} \qquad \Sigma_{YX|Z} = (\Sigma_{YX} -\Sigma_{YZ}\Sigma_{ZZ}^{-1}\Sigma_{ZX})\]

<p>Similarly to the cross-covariance operator, we have \(\Sigma_{YX\|Z}= 0 \iff X\) and $Y$ are independent given $Z$.</p>

<p>Empirically, given i.i.d samples \((x_i, y_i)_{i=1}^m\), we can denote \(k_\mathcal{X}(x_i, \cdot) = \varphi(x_i)\) and \(k_\mathcal{Y}(y_i, \cdot) = \psi(y_i)\). Now define</p>

\[\hat{\mu}_X = \frac{1}{m} \sum_{i=1}^m  \varphi(x_i) \qquad \text{and} \qquad \hat{\mu}_Y = \frac{1}{m} \sum_{i=1}^m  \psi(y_i)\]

<p>the empirical kernel mean embeddings of $X$ and $Y$. Then the empirical cross-covariance operator is</p>

\[\widehat{\Sigma}_{YX} = \frac{1}{m} \sum_{i=1}^m (\varphi(x_i) - \hat{\mu}_X) \otimes (\psi(y_i) - \hat{\mu}_Y)\]

<p>We often see the empirical cross-covariance operator denoted in matrix form, using \(\Phi = [\varphi(x_1), \dots, \varphi(x_m)]\) and \(\Psi = [\psi(y_1), \dots, \psi(y_m)]\) the feature matrices in \(\mathcal{H}_{\mathcal{X}}\) and \(\mathcal{H}_{\mathcal{Y}}\) respectively, and \(H = I - \frac{1}{m}\mathbf{1}\mathbf{1}^\top\) the centering matrix. Then</p>

\[\widehat{\Sigma}_{YX} = \frac{1}{m} \Phi H \Psi^\top\]

<hr />

<h2 id="2-testing-with-kernels">2. Testing with Kernels</h2>

<h3 id="21-maximum-mean-discrepancy-mmd">2.1 Maximum Mean Discrepancy (MMD)</h3>

<h4 id="211-theory">2.1.1 Theory</h4>

<p>The Maximum Mean Discrepancy is a statistic to test whether two distributions $\mathbb{P}$ and $\mathbb{Q}$ differ based on samples drawn from each. This is a <em>two-sample-problem</em>, and our goal is to test whether $\mathbb{P} = \mathbb{Q}$, using the MMD, an integral probability metric. The MMD uses directly the Kernel Mean Embedding introduced in section 1.2 in order to compare the two distributions.</p>

<p>Formally, we set up the problem as:</p>

<p>For two probability measures \(\mathbb{P}\) and \(\mathbb{Q}\) defined on a metric space \(\mathcal{X}\), \(\mathbb{P} = \mathbb{Q} \iff \mathbb{E}_\mathbb{P}[f(x)] = \mathbb{E}_\mathbb{Q}[f(x)] \; \forall f \in C(\mathcal{X})\), \(C(\mathcal{X})\) being the set of continuous bounded functions on \(\mathcal{X}\). We now define the test-statistic based on the class of functions in the unit ball of a Reproducing Kernel Hilbert Space \(\mathcal{H}_k\) as:</p>

<p><strong>Definition (Maximum Mean Discrepancy).</strong></p>

\[\text{MMD}_k(\mathbb{P},\mathbb{Q} ) := \sup_{\substack{f \in \mathcal{H}_k \\ \|f\|_{\mathcal{H}_k} \leq 1}} \left( \mathbb{E}_{X \sim \mathbb{P}}[f(x)] - \mathbb{E}_{Y \sim \mathbb{Q}}[f(y)] \right)\]

<p>The restriction to the unit ball \(\|f\|_{\mathcal{H}_k} \leq 1\) serves two purposes. It prevents the supremum from being infinite by bounding the function class; and more importantly, it ensures that the MMD measures genuine distributional differences rather than differences that could be amplified by arbitrary scaling. The unit ball constraint makes the MMD a proper metric, satisfying the triangle inequality to provide a meaningful notion of distance.</p>

<p>Using the reproducing property, and writing \(\text{MMD}_k(\mathbb{P},\mathbb{Q} )\) as \(\text{MMD}_k\), we obtain:</p>

\[\text{MMD}_k = \sup_{\substack{f \in \mathcal{H}_k \\ \|f\|_{\mathcal{H}_k} \leq 1}} \langle f, \mu_\mathbb{P} - \mu_\mathbb{Q} \rangle_{\mathcal{H}_k} = \|\mu_\mathbb{P} - \mu_\mathbb{Q}\|_{\mathcal{H}_k}\]

<p>This result follows from the Cauchy-Schwarz inequality, where the supremum of \(\langle f, g \rangle_{\mathcal{H}_k}\) over unit vectors $f$ is achieved when $f$ is aligned with $g$. So the MMD can be simply defined as the RKHS norm of the difference between mean embeddings. If the distributions are identical, their mean embeddings should coincide and the MMD should be 0.</p>

<p>Expanding the squared MMD, we can now obtain the kernel-based expression:</p>

\[\begin{align*}
\text{MMD}^2_k &amp;= \|\mu_\mathbb{P} - \mu_\mathbb{Q}\|^2_{\mathcal{H}_k} \\
&amp;= \langle \mu_\mathbb{P} - \mu_\mathbb{Q}, \mu_\mathbb{P} - \mu_\mathbb{Q} \rangle_{\mathcal{H}_k} \\
&amp;= \langle \mu_\mathbb{P}, \mu_\mathbb{P} \rangle_{\mathcal{H}_k} - 2\langle \mu_\mathbb{P}, \mu_\mathbb{Q} \rangle_{\mathcal{H}_k} + \langle \mu_\mathbb{Q}, \mu_\mathbb{Q} \rangle_{\mathcal{H}_k} \\
&amp;= \mathbb{E}_{X,X' \sim \mathbb{P}}[k(X, X')] - 2\mathbb{E}_{X \sim \mathbb{P}, Y \sim \mathbb{Q}}[k(X, Y)] + \mathbb{E}_{Y,Y' \sim \mathbb{Q}}[k(Y, Y')]
\end{align*}\]

<p>In practice, we work with the squared MMD for computational purposes. We can estimate it directly from samples without having to compute the feature maps, or the mean embeddings explicitly. This is another instance of the kernel trick, where we can use kernel evaluations instead of manually computing the inner products between potentially infinite-dimensional feature maps.</p>

<!-- ![Diagram](/images/KMExy_light.png) -->

<p align="center">
<img src="/images/KMExy_light.png" data-light-src="/images/KMExy_light.png" data-dark-src="/images/KMExy_dark.png" alt="Diagram" />
</p>

<p><strong>Theorem (Characteristic Kernels and MMD).</strong> If $k$ is a characteristic kernel on \(\mathcal{X}\), then \(\text{MMD}_k(\mathbb{P}, \mathbb{Q}) = 0\) if and only if \(\mathbb{P} = \mathbb{Q}\). Thus, for characteristic kernels, the MMD defines a proper metric on the space of probability measures.</p>

<p>This theorem essentially guarantees the validity of the test-statistic. We can reject the null hypothesis \(\mathbb{P} = \mathbb{Q}\) based on a large MMD value.</p>

<h4 id="212-empirical-estimation">2.1.2 Empirical Estimation</h4>

<p>Given finite samples \(\{x_1, \ldots, x_m\} \sim \mathbb{P}\) and \(\{y_1, \ldots, y_n\} \sim \mathbb{Q}\), we need to estimate \(\text{MMD}^2_k(\mathbb{P}, \mathbb{Q})\) from data. Substituting the empirical mean embeddings into the quadratic form expression yields the natural empirical estimator.</p>

<p><strong>Biased V-Statistic Estimator</strong></p>

<p>The V-statistic is obtained by directly plugging the empirical mean embeddings into the population MMD$^2$ formula:</p>

\[\widehat{\text{MMD}}_v^2 = \frac{1}{m^2} \sum_{i,j=1}^m k(x_i,x_j) - \frac{2}{mn} \sum_{i=1}^{m} \sum_{j=1}^{n} k(x_i,y_j) + \frac{1}{n^2} \sum_{i,j=1}^n k(y_i,y_j).\]

<p>This estimator is the most natural empirical analog of the population quantity and has a simple interpretation: it computes the average kernel similarity within each sample and between samples. The V-statistic can be efficiently computed using matrix operations: if \(\mathbf{K}_{XX} \in \mathbb{R}^{m \times m}\), \(\mathbf{K}_{YY} \in \mathbb{R}^{n \times n}\), and \(\mathbf{K}_{XY} \in \mathbb{R}^{m \times n}\) are the kernel Gram matrices, then</p>

\[\widehat{\text{MMD}}_v^2 = \frac{1}{m^2} \mathbf{1}^\top \mathbf{K}_{XX} \mathbf{1} - \frac{2}{mn} \mathbf{1}^\top \mathbf{K}_{XY} \mathbf{1} + \frac{1}{n^2} \mathbf{1}^\top \mathbf{K}_{YY} \mathbf{1}\]

<p><strong>Unbiased U-Statistic Estimator</strong></p>

<p>To obtain an unbiased estimator, we exclude the diagonal terms by restricting summations to distinct indices:</p>

\[\widehat{\text{MMD}}_u^2 = \frac{1}{m(m-1)} \sum_{\substack{i,j=1 \\ i \neq j}}^m k(x_i, x_j) - \frac{2}{mn} \sum_{i=1}^{m} \sum_{j=1}^{n} k(x_i, y_j) + \frac{1}{n(n-1)} \sum_{\substack{i,j=1 \\ i \neq j}}^n k(y_i, y_j).\]

<p>This estimator is unbiased: \(\mathbb{E}[\widehat{\text{MMD}}_u^2] = \text{MMD}^2(\mathbb{P}, \mathbb{Q})\) exactly for all sample sizes. It is preferred when exact unbiasedness is important, such as in small-sample settings or when theoretical guarantees on Type I error rates are needed. Like the biased estimator, it can be computed efficiently with matrix operations:</p>

\[\widehat{\text{MMD}}_u^2 = \frac{1}{m(m-1)} \mathbf{1}^\top \bar{\mathbf{K}}_{XX} \mathbf{1} - \frac{2}{mn} \mathbf{1}^\top \bar{\mathbf{K}}_{XY} \mathbf{1} + \frac{1}{n(n-1)} \mathbf{1}^\top \bar{\mathbf{K}}_{YY} \mathbf{1},\]

<p>where \(\bar{\mathbf{K}}_{XX}\), \(\bar{\mathbf{K}}_{XY}\) and \(\bar{\mathbf{K}}_{YY}\) denote the Gram matrices \(\mathbf{K}_{XX}\), \(\mathbf{K}_{XY}\), and \(\mathbf{K}_{YY}\), with diagonal entries set to 0.</p>

<p>The test thresholds used are found using the Pearson curve (fitting on its first four moments) and bootstrap methods to estimate the distribution of the statistic under the null hypothesis. Gretton et al. (2012) conclude that the Pearson curve method generally provides a more accurate estimate of the threshold for low sample sizes while the two methods provide similar estimates for larger sample sizes, thus making the Bootstrap method preferable as its computational cost is $O(m^2)$ compared to $O(m^3)$ for the Pearson Curves.</p>

<h3 id="22-hilbert-schmidt-independence-criterion-hsic">2.2 Hilbert-Schmidt Independence Criterion (HSIC)</h3>

<h4 id="221-theory">2.2.1 Theory</h4>

<p>The Hilbert-Schmidt Independence Criterion (HSIC) is a kernel-based measure of statistical dependence between two random variables. While MMD tests whether two distributions are identical, HSIC tests whether two random variables are independent by measuring the Hilbert-Schmidt norm of their cross-covariance operator introduced in Section 1.3.2.</p>

<p>For random variables \(X \in \mathcal{X}\) and \(Y \in \mathcal{Y}\) with joint distribution \(\mathbb{P}_{XY}\) and marginals \(\mathbb{P}_X\) and \(\mathbb{P}_Y\), independence is characterized by \(\mathbb{P}_{XY} = \mathbb{P}_X \otimes \mathbb{P}_Y\). The cross-covariance operator $\Sigma_{YX}$ captures the dependence structure between $X$ and $Y$ in the tensor product space \(\mathcal{H}_\mathcal{Y} \otimes \mathcal{H}_\mathcal{X}\).</p>

<p><strong>Definition (Hilbert-Schmidt Independence Criterion).</strong> Let \(k_\mathcal{X}\) and \(k_\mathcal{Y}\) be kernels on \(\mathcal{X}\) and \(\mathcal{Y}\) with RKHSs \(\mathcal{H}_\mathcal{X}\) and \(\mathcal{H}_\mathcal{Y}\). The HSIC between $X$ and $Y$ is defined as the squared Hilbert-Schmidt norm of the associated cross-covariance operator $\Sigma_{YX}$:</p>

\[\text{HSIC}(X, Y) := \|\Sigma_{YX}\|_{\text{HS}}^2\]

<p>where \(\Sigma_{YX}: \mathcal{H}_\mathcal{X} \to \mathcal{H}_\mathcal{Y}\) is the cross-covariance operator.</p>

<p><strong>Remark (Hilbert-Schmidt Norm and Singular Values).</strong> For a compact Hilbert-Schmidt operator \(C: \mathcal{H} \to \mathcal{G}\) with singular value decomposition \(C = \sum_{i=1}^\infty \sigma_i \langle \cdot, h_i \rangle_{\mathcal{H}} g_i\), where \(\{\sigma_i\}_{i=1}^\infty\) are the singular values and \((h_i)_{i \in I}\), \((g_i)_{i \in I}\) are orthonormal bases of \(\mathcal{H}\) and \(\mathcal{G}\) respectively, the Hilbert-Schmidt norm can equivalently be expressed as:</p>

\[\|C\|_{\text{HS}}^2 = \sum_{i \in I} \|Ch_i\|_{\mathcal{G}}^2 = \sum_{i=1}^\infty \sigma_i^2(C)\]

<p>The Hilbert-Schmidt norm aggregates information across all modes of the operator, capturing the total magnitude of the linear transformation. In the context of HSIC, the Hilbert-Schmidt norm measures the magnitude of the cross-covariance operator. When $X$ and $Y$ are independent, \(\Sigma_{YX} = 0\) and thus \(\text{HSIC}(X,Y) = 0\).</p>

<p>Using \(\Sigma_{YX} = \mathbb{E}_{YX}[\phi(x) \otimes \psi(y)] - \mu_{\mathbb{P}_X} \otimes \mu_{\mathbb{P}_Y}\), we can expand the Hilbert-Schmidt norm using the definition of the cross-covariance operator, and obtain the kernel-based expression:</p>

\[\begin{align*}
\text{HSIC}(X,Y) = \|\Sigma_{YX}\|_{\text{HS}}^2 &amp;= \langle \:\mathbb{E}_{YX}[\phi(x) \otimes \psi(y)] - \mu_{\mathbb{P}_X} \otimes \mu_{\mathbb{P}_Y} \:, \: \mathbb{E}_{YX}[\phi(x) \otimes \psi(y)] - \mu_{\mathbb{P}_X} \otimes \mu_{\mathbb{P}_Y} \:\rangle \\
&amp;= \mathbb{E}_{XY, X'Y'}\big[k_\mathcal{X}(X,X')k_\mathcal{Y}(Y,Y')\big]  + \mathbb{E}_{X,X'}\big[k_\mathcal{X}(X,X')\big] \mathbb{E}_{Y,Y'}\big[k_\mathcal{Y}(Y,Y')\big] \\
&amp;\qquad - 2\mathbb{E}_{XY}\big[\mathbb{E}_{X'}\big[k_\mathcal{X}(X,X')\big] \mathbb{E}_{Y'}\big[k_\mathcal{Y}(Y,Y')\big]\big]
\end{align*}\]

<p>where the expectations are taken over independent copies $(X,Y)$ and $(X’,Y’)$. This expression involves only kernel evaluations and expectations, enabling empirical estimation without explicit feature map computations. This is another instance of the kernel trick, allowing us to work directly with kernel evaluations in potentially infinite-dimensional feature spaces.</p>

<h4 id="222-empirical-estimation">2.2.2 Empirical Estimation</h4>

<p>Given finite paired samples \(\{(x_i, y_i)\}_{i=1}^m\) drawn i.i.d. from \(\mathbb{P}_{XY}\), we estimate \(\text{HSIC}(X,Y)\) from data. Following the MMD framework, we can derive both biased and unbiased estimators based on the empirical cross-covariance operator.</p>

<p><strong>Biased V-Statistic Estimator</strong></p>

<p>The biased estimator is obtained by directly substituting the empirical cross-covariance operator into the Hilbert-Schmidt norm:</p>

\[\widehat{\text{HSIC}}_v = \frac{1}{m^2} \text{tr}(\mathbf{K}_{XX} \mathbf{H} \mathbf{K}_{YY} \mathbf{H})\]

<p>where \(\mathbf{K}_{XX} \in \mathbb{R}^{m \times m}\) and \(\mathbf{K}_{YY} \in \mathbb{R}^{m \times m}\) are the kernel Gram matrices, and \(\mathbf{H} = \mathbf{I} - \frac{1}{m}\mathbf{1}\mathbf{1}^\top\) is the centering matrix.</p>

<p>Similarly to the Maximum Mean Discrepancy estimators, we can also define an unbiased U-statistic estimator of the Hilbert-Schmidt Independence Criterion.</p>

<p><strong>Unbiased U-Statistic Estimator</strong></p>

<p>An unbiased estimator of the HSIC is given by:</p>

\[\widehat{\text{HSIC}}_u = \frac{1}{m(m-3)} \left[ \text{tr}(\bar{\mathbf{K}}_{XX} \bar{\mathbf{K}}_{YY}) + \frac{\mathbf{1}^\top \bar{\mathbf{K}}_{XX} \mathbf{1} \cdot \mathbf{1}^\top \bar{\mathbf{K}}_{YY} \mathbf{1}}{(m-1)(m-2)} - \frac{2}{m-2} \mathbf{1}^\top \bar{\mathbf{K}}_{XX} \bar{\mathbf{K}}_{YY} \mathbf{1} \right]\]

<p>where \(\bar{\mathbf{K}}_{XX}\) and \(\bar{\mathbf{K}}_{YY}\) denote the Gram matrices \(\mathbf{K}_{XX}\) and \(\mathbf{K}_{YY}\), respectively, with diagonal entries set to 0. This estimator satisfies \(\mathbb{E}[\widehat{\text{HSIC}}_u] = \text{HSIC}(X,Y)\). The U-statistic is preferred in small-sample settings or when exact unbiasedness is required for hypothesis testing.</p>

<h4 id="223-extensions-of-hsic">2.2.3 Extensions of HSIC</h4>

<p>We can use a similar framework to test conditional independence, putting together the HSIC for cross-covariance and the normalized cross-covariance operators introduced in Definition 12. We can then define both the normalized HSIC and the normalized conditional HSIC, following the work and notation of K. Fukumizu, to which we refer the reader for more details, justifications and convergence proofs.</p>

<p><strong>Definition (Conditional HSIC).</strong> The conditional HSIC and normalized HSIC are defined as:</p>

\[\begin{align*}
I^{COND}(X,Y|Z)  &amp;:= \|V_{YX|Z}\|_{\text{HS}}^2\\
I^{NOCCO}(X,Y)  &amp;:= \|V_{YX}\|_{\text{HS}}^2
\end{align*}\]

<p>where \(V_{YX\|Z} = V_{YX} - V_{YZ}V_{XZ}\) is the normalized conditional cross-covariance operator, and $V_{YX}$ the normalized cross-covariance operator, as defined in section 1.3.2.</p>

<p>For characteristic kernels, \(I^{COND}(X, Y \mid Z) = 0\) if and only if \(X \perp Y \mid Z\), and \(I^{NOCCO}(X,Y) = 0\) if and only if $X \perp Y$. These measures provide a complete characterization of conditional and unconditional independence, respectively.</p>

<p><strong>Empirical Estimation</strong></p>

<p>Given i.i.d. samples \(\{(x_i, y_i, z_i)\}_{i=1}^m\) from the joint distribution \(\mathbb{P}_{XYZ}\), we can construct empirical estimators of both measures. We first define the centered Gram matrices \(\mathbf{\tilde{K}}_{XX}, \mathbf{\tilde{K}}_{YY}, \mathbf{\tilde{K}}_{ZZ} \in \mathbb{R}^{m \times m}\) with entries</p>

\[K_{XX,ij} = \langle k_\mathcal{X}(\cdot, x_i) - \hat{\mu}_{\mathbb{P}_X} \:, \: k_\mathcal{X}(\cdot, x_j) - \hat{\mu}_{\mathbb{P}_X}\rangle_{\mathcal{H}_\mathcal{X}}\]

<p>and similarly for \(\mathbf{\tilde{K}}_{YY}\) and \(\mathbf{\tilde{K}}_{ZZ}\), where \(\hat{\mu}_{\mathbb{P}_X}\) are the empirical mean embeddings. To ensure numerical stability, we introduce regularized matrices through the regularization parameter $\epsilon_m &gt; 0$:</p>

\[\mathbf{R}_X = \mathbf{\tilde{K}}_{XX}(\mathbf{\tilde{K}}_{XX} + m\epsilon_m \mathbf{I})^{-1}, \quad \mathbf{R}_{Y} = \mathbf{\tilde{K}}_{YY}(\mathbf{\tilde{K}}_{YY} + m\epsilon_m \mathbf{I})^{-1}, \quad \mathbf{R}_{Z} = \mathbf{\tilde{K}}_{ZZ}(\mathbf{\tilde{K}}_{ZZ} + m\epsilon_m \mathbf{I})^{-1}\]

<p>The empirical normalized HSIC is then computed as:</p>

\[\widehat{I}^{NOCCO}_m(X,Y) = \text{tr}(\mathbf{R}_Y \mathbf{R}_X)\]

<p>For conditional HSIC, using the empirical normalized conditional cross-covariance operator, the estimator is:</p>

\[\widehat{I}^{COND}_m(X,Y|Z) = \text{tr}(\mathbf{R}_Y \mathbf{R}_X - 2\mathbf{R}_Y \mathbf{R}_X \mathbf{R}_Z + \mathbf{R}_Y \mathbf{R}_Z \mathbf{R}_X \mathbf{R}_Z)\]

<h3 id="23-kernel-stein-discrepancy-ksd">2.3 Kernel Stein Discrepancy (KSD)</h3>

<p>Stein’s method was introduced by James-Stein in the 1970s, but the subsequent developments and applications of his method to non-linear feature maps and more modern statistical learning and hypothesis testing have resurfaced in the 2010s. For this section, we refer mostly to Chwialkowski et al. (2016), Gorham &amp; Mackey (2015), and Liu et al. (2016) for additional details and proofs on the methods introduced.</p>

<h4 id="231-steins-method">2.3.1 Stein’s Method</h4>

<p>Stein’s method provides an elegant solution for testing whether samples are drawn from a target distribution, in the case where the target is only known up to a normalizing constant, which might be intractable to compute. Classical goodness-of-fit tests, such as Kolmogorov-Smirnov or likelihood-based methods required access to the normalized target density. Maximum Mean Discrepancy requires samples from both distributions, which we may not have for the target.</p>

<p>Stein’s method relies on an identity that eliminates the dependence on the normalizing constant. We first introduce the <em>Stein Operator</em>.</p>

<p><strong>Definition (Stein Operator).</strong> For a smooth probability density $p$ on \(\mathbb{R}^d\), the Stein operator $\mathcal{T}_p$ is the linear operator acting on smooth vector-valued test functions \(f : \mathbb{R}^d \to \mathbb{R}^d\) defined by:</p>

\[\mathcal{T}_p f(x) := \nabla_x \log p(x) \cdot f(x) + \nabla_x \cdot f(x),\]

<p>where \(\nabla_x \log p(x) \cdot f(x) = \sum_{i=1}^d \frac{\partial \log p}{\partial x_i} f_i(x)\) denotes the Euclidean inner product, and $\nabla_x \cdot f(x) = \sum_{i=1}^d \frac{\partial f_i}{\partial x_i}$ is the divergence of $f$.</p>

<p>The crucial property of this operator is that the score function $\nabla_x \log p(x)$ depends only on the unnormalized density. This operator is particularly useful because it depends on $p$ only through the <em>score function</em> \(\nabla_x \log p(x)\). If \(p(x) = \tilde{p}(x)/Z\) where $Z$ is an intractable normalizing constant, then:</p>

\[\nabla_x \log p(x) = \nabla_x \log \frac{\tilde{p}(x)}{Z} = \nabla_x \log \tilde{p}(x) - \nabla_x \log Z = \nabla_x \log \tilde{p}(x)\]

<p>since $Z$ is a constant. Thus, the identity can be evaluated without knowledge of the normalizing constant. We now define Stein’s identity:</p>

<p><strong>Theorem (Stein’s Identity).</strong> Let $p$ be a smooth probability density on \(\mathbb{R}^d\) with \(\nabla_x p(x) \to 0\) as \(\|x\| \to \infty\). For any smooth vector-valued function \(f : \mathbb{R}^d \to \mathbb{R}^d\) that decays sufficiently fast at infinity,</p>

\[\mathbb{E}_{x \sim p}\left[ \mathcal{T}_p f(x) \right] = 0\]

<p><em>Proof sketch (1d case).</em></p>

\[\begin{align*}
\mathbb{E}_{x \sim p}\left[\frac{p'(x)}{p(x)} f(x) + f'(x)\right] &amp;= \int_{-\infty}^{\infty} \left[p'(x) f(x) + p(x) f'(x)\right] dx \\
&amp;= \int_{-\infty}^{\infty} \frac{d}{dx}[p(x) f(x)] dx \\
&amp;= [p(x) f(x)]_{-\infty}^{\infty} = 0
\end{align*}\]

<p>where the last equality follows from the boundary conditions $p(x) \to 0$ as $x \to \pm\infty$. The multivariate case follows by applying this argument component-wise. $\square$</p>

<p>Stein’s identity provides a principled approach to test discrepancies without requiring a normalizing constant nor samples from the target distribution. If samples are drawn from $p$, then \(\mathbb{E}_{x \sim p}[\mathcal{T}_p f(x)] = 0\) for all suitable $f$. Conversely, given samples \(\{x_i\}_{i=1}^n\) from an unknown distribution $q$, we can evaluate \(\mathbb{E}_{x \sim q}[\mathcal{T}_p f(x)]\), which we expect to be $0$ in the case where $q = p$. The magnitude of the Stein discrepancy \(\mathbb{E}_{x \sim q}[\mathcal{T}_p f(x)]\) should provide evidence against the hypothesis that $q = p$. This raises the question of the class of functions one should consider for $f$. We’ll see in the next section that we can apply a procedure similar to the framework of the Maximum Mean Discrepancy statistic in section 2.1.1, using a kernel-based approach by considering all functions in the unit ball of a RKHS.</p>

<h4 id="232-ksd">2.3.2 KSD</h4>

<p>Following the framework of the Maximum Mean Discrepancy, we adopt a kernel-based approach by considering all functions in the unit ball of a reproducing kernel Hilbert space.</p>

<p>Let \(\mathcal{H}^d = \mathcal{H} \times \cdots \times \mathcal{H}\) be the product RKHS of vector-valued functions \(f: \mathbb{R}^d \to \mathbb{R}^d\), where \(\mathcal{H}\) is an RKHS with kernel \(k: \mathbb{R}^d \times \mathbb{R}^d \to \mathbb{R}\).</p>

<p>Using the derivative reproducing property (Proposition 3) on the product space $\mathcal{H}^d$, note that:</p>

\[\nabla^\top f(x) = \langle f, \nabla k(x, \cdot)\rangle_{\mathcal{H}^d} \qquad \text{and} \qquad f(x)^\top \nabla \log p(x) = \langle f, \nabla \log p(x) k(x, \cdot )\rangle_{\mathcal{H}^d} \qquad \forall f\in \mathcal{H}^d, x\in \mathbb{R}^d\]

<p>We can now express the Stein operator as an inner product within $\mathcal{H}^d$ using once more the reproducing property:</p>

\[\mathcal{T}_p f(x) = \nabla_x \log p(x) \cdot f(x) + \nabla_x \cdot f(x) = \langle f, \nabla \log p(x) k(x, \cdot ) +\nabla k(x, \cdot)\rangle_{\mathcal{H}^d}\]

<p>And following the notation of Chwialkowski et al. (2016), writing \(\xi_p (x, \cdot) := \nabla \log p(x) k(x, \cdot ) +\nabla k(x, \cdot)\), we can now define the expected Stein operator for samples from a distribution $q$ as an inner product in the RKHS:</p>

\[\mathbb{E}_{x \sim q}\left[ \mathcal{T}_p f(x) \right] = \mathbb{E}_{x \sim q}\left[\langle f, \xi_p (x, \cdot) \rangle \right] = \langle f, \mathbb{E}_{x \sim q}\left[\xi_p (x, \cdot)\right] \rangle\]

<p>We finally define the <em>Kernelized Stein Discrepancy</em> as the supremum of the expected Stein operator over the class of all functions in the unit ball of the RKHS $\mathcal{H}^d$.</p>

<p><strong>Definition (Kernelized Stein Discrepancy).</strong> For a distribution $q$ on $\mathbb{R}^d$ and a target distribution $p$, the Kernelized Stein Discrepancy is defined as:</p>

\[\text{KSD}(q, p) := \sup_{\substack{f \in \mathcal{H}^d \\ \|f\|_{\mathcal{H}^d} \leq 1}} \mathbb{E}_{x \sim q}[\mathcal{T}_p f(x)] = \sup_{\substack{f \in \mathcal{H}^d \\ \|f\|_{\mathcal{H}^d} \leq 1}} \langle f, \mathbb{E}_{x \sim q}\left[\xi_p (x, \cdot)\right] \rangle  = \| \mathbb{E}_{x \sim q}\left[\xi_p (x, \cdot) \right]\|_{\mathcal{H}^d}\]

<p>using Cauchy-Schwarz, similarly as for the derivation of the closed-form of the Maximum Mean Discrepancy in section 2.1.1.</p>

<p>We can now express the squared KSD as:</p>

\[\text{KSD}^2(q, p) = \| \mathbb{E}_{x \sim q}\left[\xi_p (x, \cdot) \right]\|_{\mathcal{H}^d}^2 = \mathbb{E}_{x \sim q, x' \sim q}\left[ h_P(x, x') \right]\]

<p>where $h_P$ is the <em>Stein Kernel</em>, defined as:</p>

\[\begin{align*}
h_P(x, x') &amp;:= \nabla \log p(x)^\top\nabla \log p(x') k(x, x') \\
&amp;\quad+ \nabla \log p(x')^\top\nabla_x k(x, x') \\
&amp;\quad+ \nabla \log p(x)^\top\nabla_{x'} k(x, x') \\
&amp;\quad+ \langle \nabla_x k(x, \cdot), \nabla_{x'} k(\cdot, x')\rangle_{\mathcal{H}^d}
\end{align*}\]

<p>Once again, this enables us to use the “kernel trick” in order to evaluate the (squared) Kernel Stein Discrepancy using only kernel evaluations and the score function $\nabla_x \log p(x)$ of the unnormalized target density.</p>

<h4 id="233-empirical-estimation">2.3.3 Empirical Estimation</h4>

<p>Similarly to the Maximum Mean Discrepancy and Hilbert-Schmidt Independence Criterion chapters, we now focus on the empirical estimation of the Kernel Stein Discrepancy statistic from data samples. Given finite samples ${x_1, \ldots, x_m}$ drawn i.i.d. from an unknown distribution $q$, we can estimate $\text{KSD}^2(q, p)$ from data using empirical averages of the Stein kernel.</p>

<p><strong>Biased V-Statistic Estimator</strong></p>

<p>The V-statistic estimator is obtained by directly substituting the empirical distribution into the population formula:</p>

\[\widehat{\text{KSD}}_v^2 = \frac{1}{m^2}\sum_{i,j=1}^m h_P(x_i, x_j)\]

<p>This estimator is the empirical analog of \(\text{KSD}^2(q, p) = \mathbb{E}_{x, x' \sim q}[h_P(x, x')]\). The V-statistic includes diagonal terms $h(x_i, x_i)$ and is therefore biased for finite samples. Setting $\mathbf{H}_{XX}$ as the kernel Gram matrix of the Stein kernel $h_P$, we can also express it in matrix form:</p>

\[\widehat{\text{KSD}}_v^2 = \frac{1}{m^2} \mathbf{1}^\top \mathbf{H}_{XX}\mathbf{1}\]

<p><strong>Unbiased U-Statistic Estimator</strong></p>

<p>To obtain an unbiased estimator, we exclude the diagonal terms, denoting by \(\mathbf{\tilde{H}}_{XX}\) the Stein Kernel Gram matrix \(\mathbf{H}_{XX}\) with its diagonal elements set to 0:</p>

\[\widehat{\text{KSD}}_u^2 = \frac{1}{m(m-1)}\sum_{\substack{i,j=1 \\ i\neq j}}^m h_P(x_i, x_j) \qquad \text{and its matrix form} \quad \widehat{\text{KSD}}_u^2 = \frac{1}{m(m-1)} \mathbf{1}^\top \mathbf{\tilde{H}}_{XX}\mathbf{1}\]

<p>This estimator satisfies $\mathbb{E}[\widehat{\text{KSD}}_u^2] = \text{KSD}^2(q, p)$. Similarly to the MMD and HSIC U-statistics, this estimator is preferred when exact unbiasedness is required, such as in small-sample settings.</p>

<p>To test the null hypothesis $H_0: q = p$ versus $H_1: q \neq p$, we compare the empirical KSD against a threshold, generally defined as a quantile of its distribution over the null-hypothesis, obtained via the Wild Bootstrap method.</p>

<hr />
<h2 id="references">References</h2>

<p><strong>Theory of Reproducing Kernels</strong> (1950)<br />
Nachman Aronszajn<br />
<a href="https://www.ams.org/journals/tran/1950-068-03/S0002-9947-1950-0051437-7/S0002-9947-1950-0051437-7.pdf" target="_blank">Paper</a></p>

<p><strong>Reproducing Kernel Hilbert Spaces in Probability and Statistics</strong> (2011)<br />
Alain Berlinet, Christine Thomas-Agnan<br />
<a href="https://link.springer.com/book/10.1007/978-1-4419-9096-9" target="_blank">Book</a></p>

<p><strong>A Kernel Test of Goodness of Fit</strong> (2016)<br />
Kacper Chwialkowski, Heiko Strathmann, Arthur Gretton<br />
<a href="https://arxiv.org/abs/1602.02964" target="_blank">Paper</a></p>

<p><strong>Dimensionality Reduction for Supervised Learning with Reproducing Kernel Hilbert Spaces</strong> (2004)<br />
Kenji Fukumizu, Francis R. Bach, Michael I. Jordan<br />
<a href="https://www.jmlr.org/papers/v5/fukumizu04a.html" target="_blank">Paper</a></p>

<p><strong>Kernel Measures of Conditional Dependence</strong> (2007)<br />
Kenji Fukumizu, Arthur Gretton, Xiaohai Sun, Bernhard Schölkopf<br />
<a href="https://papers.nips.cc/paper_files/paper/2007/file/3a0772443a0739141292a5429b952fe6-Paper.pdf" target="_blank">Paper</a></p>

<p><strong>Kernel Dimension Reduction in Regression</strong> (2009)<br />
Kenji Fukumizu, Francis R. Bach, Michael I. Jordan<br />
<a href="https://doi.org/10.1214/08-AOS637" target="_blank">Paper</a></p>

<p><strong>Measuring Sample Quality with Stein’s Method</strong> (2015)<br />
Jackson Gorham, Lester Mackey<br />
<a href="https://arxiv.org/abs/1506.03039" target="_blank">Paper</a></p>

<p><strong>A Kernel Method for the Two-Sample Problem</strong> (2007)<br />
Arthur Gretton, Karsten M. Borgwardt, Malte Rasch, Bernhard Schölkopf, Alexander Smola<br />
<a href="https://arxiv.org/abs/0805.2368" target="_blank">Paper</a></p>

<p><strong>Measuring Statistical Dependence with Hilbert-Schmidt Norms</strong> (2005)<br />
Arthur Gretton, Olivier Bousquet, Alex Smola, Bernhard Schölkopf<br />
<a href="https://link.springer.com/chapter/10.1007/11564089_7" target="_blank">Paper</a></p>

<p><strong>A Kernel Two-Sample Test</strong> (2012)<br />
Arthur Gretton, Karsten M. Borgwardt, Malte J. Rasch, Bernhard Schölkopf, Alexander Smola<br />
<a href="https://www.jmlr.org/papers/v13/gretton12a.html" target="_blank">Paper</a></p>

<p><strong>A Kernelized Stein Discrepancy for Goodness-of-fit Tests</strong> (2016)<br />
Qiang Liu, Jason Lee, Michael Jordan<br />
<a href="https://arxiv.org/abs/1602.03253" target="_blank">Paper</a></p>

<p><strong>Methods of Modern Mathematical Physics I: Functional Analysis</strong> (1980)<br />
Michael Reed, Barry Simon<br />
<a href="https://www.astrosen.unam.mx/~aceves/Metodos/ebooks/reed_simon1.pdf" target="_blank">Book</a></p>

<p><strong>Learning with Kernels</strong> (1998)<br />
Alexander J. Smola, Bernhard Schölkopf<br />
<a href="https://mcube.lab.nycu.edu.tw/~cfung/docs/books/scholkopf2002learning_with_kernels.pdf" target="_blank">Book</a></p>

<p><strong>A Hilbert Space Embedding for Distributions</strong> (2007)<br />
Alex Smola, Arthur Gretton, Le Song, Bernhard Schölkopf<br />
<a href="https://link.springer.com/chapter/10.1007/978-3-540-75225-7_5" target="_blank">Paper</a></p>

<p><strong>Supervised Feature Selection via Dependence Estimation</strong> (2007)<br />
Le Song, Alex Smola, Arthur Gretton, Karsten M. Borgwardt, Justin Bedo<br />
<a href="https://dl.acm.org/doi/10.1145/1273496.1273600" target="_blank">Paper</a></p>

<p><strong>Hilbert Space Embeddings and Metrics on Probability Measures</strong> (2010)<br />
Bharath K. Sriperumbudur, Arthur Gretton, Kenji Fukumizu, Bernhard Schölkopf, Gert R. G. Lanckriet<br />
<a href="https://www.jmlr.org/papers/v11/sriperumbudur10a.html" target="_blank">Paper</a></p>

<p><strong>A Bound for the Error in the Normal Approximation to the Distribution of a Sum of Dependent Random Variables</strong> (1972)<br />
Charles Stein<br />
<a href="https://projecteuclid.org/euclid.bsmsp/1200514239" target="_blank">Paper</a></p>

<p><strong>Support Vector Machines</strong> (2008)<br />
Ingo Steinwart, Andreas Christmann<br />
<a href="https://link.springer.com/book/10.1007/978-0-387-77242-4" target="_blank">Book</a></p>

<p><strong>Derivative Reproducing Properties for Kernel Methods in Learning Theory</strong> (2008)<br />
Ding-Xuan Zhou<br />
<a href="https://www.sciencedirect.com/science/article/pii/S0377042707004657" target="_blank">Paper</a></p>]]></content><author><name></name></author><summary type="html"><![CDATA[This blog post provides an introduction to kernel methods in modern statistical learning, starting from the foundational theory of reproducing kernel Hilbert spaces (RKHS), then developing on the theory of mean embeddings and covariance operators. Three major kernel-based hypothesis testing frameworks: Maximum Mean Discrepancy, the Hilbert-Schmidt Independence Criterion, and the Kernel Stein Discrepancy are introduced.]]></summary></entry><entry><title type="html">Sorting Algos</title><link href="http://localhost:4000/blog/SortingAlgos/" rel="alternate" type="text/html" title="Sorting Algos" /><published>2000-01-05T00:00:00+00:00</published><updated>2000-01-05T00:00:00+00:00</updated><id>http://localhost:4000/blog/SortingAlgos</id><content type="html" xml:base="http://localhost:4000/blog/SortingAlgos/"><![CDATA[<p>I never wrote them down. So I’m typing them out here. I also tend to forget about those so I’ll just use this to refresh my memory 10 minutes before my next pair-coding interview. I’ll also do a bit of complexity comparison between these algos, see if there’s anything unexpected.</p>

<p>There are 5 main sorting algorithms more ML/SWE/Quant compagnies will expect any candidate at any level to understand. I’ll provide an intuitive explanation about each algorithm and give a code snippet (in Python, of course — you thought I was a dev?). This will still be somewhat high-level and shouldn’t be taken as a reference for CS guys trying to become SWEs, as the provided code is not meant to be computationally optimal but rather intuitive.</p>

<hr />

<h2 id="quicksort">QuickSort</h2>

<p>Quicksort is a <em>Divide and Conquer</em> algorithm, breaking down the problem into smaller sub-problems. The process is simple:</p>
<ul>
  <li>Pick a pivot: this is an element in the array</li>
  <li>Partition the array: split the array into two sub-arrays, around the pivot. Elements smaller than the pivot should be in the left sub-array, elements larger in the right sub-array.</li>
  <li>Recursively call the same function on the two subarrays until there’s only one element in the sub-array.</li>
</ul>

<pre><code class="language-python">def quick_sort(array):

    if len(array) &lt;=1:
        # Base Case: if there's only 1 element in the array, it is sorted. 
        # We can return it as is
        return array
    
    # Select a pivot. We take the middle element here
    pivot_index = len(array)//2
    pivot = array[pivot_index]
    # take all the elements that are equal to the pivot
    pivots = [x for x in array if x==pivot]
    

    # Partition the array
    smaller = [x for x in a if x&lt;pivot]
    larger = [x for x in a if x&gt;pivot]
    
    # Recursively call
    return quicksort(smaller) + pivots + quicksort(larger)
</code></pre>

<p><strong><em>Time Complexity</em></strong>:<br />
The average runtime complexity of this algorithm is O($n$ log $n$). 
At each call, it takes O($n$) to traverse the array to select the pivots, O($n$) to fill each of the subarray.
Ideally, the pivot is the median element, which will make the two partition function divide the arary into two halves. So there will be  O($n$ log $n$) total calls.</p>

<p>However, as the pivot is not guaranteed to the be the median, the sorting could be very slow and can have O($n^2$) worst-case complexity.</p>

<p><strong><em>Space Complexity</em></strong>:<br />
O(log $n$).</p>

<hr />

<h2 id="bubblesort">BubbleSort</h2>

<p>BubbleSort is arguably the simplest algorithm.</p>

<ul>
  <li>We start at the begining of the array, and swap the first two elements if the first is greater than the second. We then go up to the next pair, until we do a full pass of the array. After the first pass, the maximum element goes to the end.</li>
  <li>We start again from the first element. After $k$ passes, the $k$ largest elements must have been moved to the last $k$ positions</li>
</ul>

<pre><code class="language-python">def bubble_sort(array):

    n = len(array)

    if n&lt;=1:
        return array
    
    # Traverse through the entire array
    for i in range(n):

        # The last i elements should already be in place
        for j in range(i, n-1-i):

            # Swap the elements if the first is larger
            if array[i]&gt;array[j]:
                array[i], array[j] = array[j], array[i]
                
    return array
</code></pre>

<p><strong><em>Time Complexity</em></strong>:<br />
O($n^2$), which makes it very slow for large datasets. We must traverse the array $n$ times and make $n-i-1$ (still O($n$)) comparisons per traversal. If the array is already sorted, its complexity reduces to O($n$).</p>

<p><strong><em>Space Complexity</em></strong>:<br />
O(1). We modify the array in place so it doesn’t require any additional memory space.</p>

<hr />

<h2 id="insertionsort">InsertionSort</h2>

<p>InsertionSort is another simple algorithm that iteratively sorts the array. It works by separating the array into a sorted one (which corresponds only to the first element at the start) and iteratively adding elements from the unsorted sub-array (on the right of the sorted sub-array) into the sorted one, at its correct position, to increase the size of the sorted sub-array by 1.</p>

<ul>
  <li>Compare the second element with the first, and swap them if the second is smaller</li>
  <li>Move to the third element, compare it to the first two elements and swap them if needed</li>
  <li>Repeat until sorted</li>
</ul>

<pre><code class="language-python">def insertion_sort(a):
    
    n = len(array)

    if n&lt;=1:
        return array
    
    # Traverse through the entire array
    for i in range(n):

        # Loop over all previous elements
        # And swap them if the order is wrong
        j=i-1
        while j&gt;=0 and array[i]&lt;array[j]:
            array[i], array[j] = array[j], array[i]
            i-=1
            j-=1

    return array
</code></pre>

<p><strong><em>Time Complexity</em></strong>:<br />
O($n^2$), which makes it very slow for large datasets. We must traverse the array $n$ times and make $i-1$ (still O($n$)) comparisons per traversal.</p>

<p><strong><em>Space Complexity</em></strong>:<br />
O(1). We modify the array in place so it doesn’t require any additional memory space.</p>

<p>One must note that InsertionSort is generally less efficient than other sorting algorithms and is only used when the list is small or nearly sorted. If the array is already sorted, its complexity reduces to O($n$).</p>

<hr />

<h2 id="mergesort">MergeSort</h2>

<p>MergeSort is one of the most popular and elegant algorithms, known for its efficiency and stability. Similarly to QuickSort, it follows a <em>Divide and Conquer</em> approach, where the array is iteratively divided into two halves which are recursively sorted and merged back together.</p>

<ul>
  <li>Divide: divide the array into two halves</li>
  <li>Conquer: sort each subarray individually</li>
  <li>Merge: Sorted subarrays are merged back in sorted order</li>
</ul>

<pre><code class="language-python">def merge(left,right):
    # This is the merge funtion
    # It merges two already sorted arrays to produce a sorted output

    # If one array is empty, we can just return the other
    if left==[]:
        return right
    if right ==[]:
        return left

    # We compare the first elements of each array
    # Add the smallest one to the final merged array 
    # And continue merging the two remaining sub-arrays
    if left[0]&lt;=right[0]:
        return [left[0]] + merge(left[1:], right)
    if left[0]&gt;right[0]:
        return [right[0]] + merge(left, right[1:])


def merge_sort(array):
    n = len(array)

    if n&lt;=1:
        return array

    # Take a pivot element to split the array
    mid = len(a)//2

    # Recusively call the function on each sub-array
    left = merge_sort(a[:mid])
    right = merge_sort(a[mid:])    

    # Merge the two sub-arrays
    return merge(left, right)
</code></pre>

<p><strong><em>Time Complexity</em></strong>:<br />
O($n$ log $n$), making it more efficient than BubbleSort and InsertionSort, and as efficient as QuickSort.
The merge function has O($n$) complexity as it has two traverse linearly both sub-arrays to merge them. The splitting in half and recursive procedure is O(log $n$ ), as usual. While having the same average complexity as QuickSort, it has O($n$ log $n$) worst-case complexity, as we are guaranteed to divide the array exactly in half at each recursion, while for QuickSort it depends on if the pivot element is close to the median.</p>

<p><strong><em>Space Complexity</em></strong>:<br />
O($n$), as additional space is required for he temporary used during merging. QuickSort is generally preferred for large datasets as it works in place and only requires  O(log $n$) memory.</p>

<hr />

<h2 id="heapsort">HeapSort</h2>

<p>HeapSort is an algorithm based on the <strong><em>Binary Heap</em></strong> data structure.</p>

<p>A <strong><em>Binary Heap</em></strong> is a binary tree with two constraints: it must be <em>complete</em>, meaning that all levels (except possibly the last one) are fully filled (and the last one is filled from left to right); and has the <em>Heap property</em>, where the key stored in each node must be greater than or equal (or lesser than or equal, depending on whether we work with a min-heap or a max-heap) than the keys in its children nodes.<br />
It is a popular data structure as its two main operations, <em>Insert</em> (adding an element) and <em>Extract</em> (removing the top element, either the largest or the smallest depending on whether we work with a min-heap or max-heap) have both O(log($n$)) time complexity. This includes, of course, the re-arrangement of the <em>Heap property</em> (through an algorithm called <strong><em>Heapify</em></strong>).<br />
Actually, the Binary Heap was invented for the HeapSort algorithm. We truly are blessed.</p>

<ul>
  <li>First convert the array into a Max Heap using Heapify</li>
  <li>Extract the root note (largest element), and replace it with the last node, and heaipfy again</li>
  <li>Repeat until the heap has only one element</li>
</ul>

<p>So the magic of this algorithm is really the <strong><em>Heapify</em></strong> procedure. Intuitively, every node has two children, and heapify works by pushing a node downward until it satisfies the heap property (i.e. it is at least as large as both of its children). Since the heap is a complete binary tree, each time a node moves down one level, the number of nodes in that level doubles, but the height of the tree is only O(log $n$). A comparison between a parent and its children is O($1$), and in the worst case heapify keeps moving the element down level by level, giving an overall cost of O(log $n$) for a single heapify call.</p>

<pre><code class="language-python">def heapify(array, n, i):
    
    # Initialize at root
    largest = i

    # Index of left child
    left = 2 * i + 1

    # Index of right child
    right = 2 * i + 2
    
    # If the left child is larger than the root
    if left &lt; n and array[left] &gt; array[largest]:
        largest = left
        
    # If the right child is larger than the root
    if right &lt; n and array[right] &gt; array[largest]:
        largest = right
        
    # If the largest is not root
    if largest != i:
        array[i], array[largest] = array[largest], array[i]

        #Recusively heapfiy the sub-tree
        heapify(arr, n, largest)

def heap_sort(array):
    n = len(array)

    if n&lt;=1:
        return array
    
    # Build max heap
    for i in range(n // 2 - 1, -1, -1):
        heapify(array, n, i)
    
    # Extract elements from heap one by one
    for i in range(n - 1, 0, -1):

        # Move current root to end
        array[i], array[0] = array[0], array[i]  

        # Heapify the reduced heap
        heapify(array, i, 0)
    
    return array
</code></pre>

<p><strong><em>Time Complexity</em></strong>:<br />
O($n$ log $n$) in all cases. Each Heapify calls has O(log $n$) runtime, and we call it everytime we extract the largest element, which we do $n$ times. This makes it quite suitable for large datasets.</p>

<p><strong><em>Space Complexity</em></strong>:<br />
O(log $n$), due to the recursive call stack.</p>

<hr />

<p>I should probably add a section about Radix Sort or Counting sort. I’ll do it. But later.</p>

<hr />
<h2 id="references">References</h2>

<p><strong>Cracking the Coding Interview</strong> (2008)<br />
Gayle Laakmann McDowell<br />
<a href="https://www.crackingthecodinginterview.com/" target="_blank">Book</a></p>

<p><strong>GeeksforGeeks</strong><br />
<a href="https://www.geeksforgeeks.org/dsa/sorting-algorithms/" target="_blank">Website</a></p>]]></content><author><name></name></author><category term="misc" /><summary type="html"><![CDATA[I never wrote them down. So I’m typing them out here. I also tend to forget about those so I’ll just use this to refresh my memory 10 minutes before my next pair-coding interview. I’ll also do a bit of complexity comparison between these algos, see if there’s anything unexpected.]]></summary></entry><entry><title type="html">Clustering</title><link href="http://localhost:4000/blog/Clustering/" rel="alternate" type="text/html" title="Clustering" /><published>2000-01-04T00:00:00+00:00</published><updated>2000-01-04T00:00:00+00:00</updated><id>http://localhost:4000/blog/Clustering</id><content type="html" xml:base="http://localhost:4000/blog/Clustering/"><![CDATA[<h2 id="k-means">K-means</h2>

<p>Given a set of $n$ observations, <em>k-means clustering</em> aims to partition the $n$ observations into $k\leq n$ sets, to minimize the <em>within-cluster sum of squares</em> (i.e. variance). The objective is:</p>

\[\text{argmin}_{S} \sum_{i=1}^k \sum_{x\in S_i} \| x - \mu_i\|^2 = \text{argmin}_{S} \sum_{i=1}^k |S_i| \text{Var}(S_i)\]

\[\text{where} \quad  \mu_i = \frac{1}{|S_i|} \sum_{x \in S_i}x \quad \small{\text{is the centroid of points in }} S_i\]

<h3 id="lloyds-algorithm-">Lloyd’s algorithm :</h3>
<blockquote>
  <ul>
    <li>Initialize: Pick K centroids $m_1, m_2, \dots, m_k$</li>
    <li>Repeat until convergence:
      <ul>
        <li><strong><em>Asssignment step</em></strong>: Assign each observation to the cluster with the nearest mean (centroid)
    \(S_i = \big\{ x_p: \| x_p - m_i\|^2 \leq \| x_p - m_j\|^2 \quad \forall j, 1\leq j\leq k \big\}\)</li>
        <li><strong><em>Update step</em></strong>: Recalculate means (centroids) for observations assigned to each cluster
    \(m_i = \frac{1}{|S_i|} \sum_{x_j \in S_i} x_j\)</li>
      </ul>
    </li>
  </ul>
</blockquote>

<p>Lloyd’s algorithm has guaranteed finite convergence to <em>local</em> minimum, as the loss $J$ (see below) decreases monotonically, is bounded below by $0$ and the partition space is finite (at most $K^n$ possible assignments).</p>

<p><strong>Mean is the Optimal Centroid</strong></p>

<p>\(\textit{Proof:}\)
Let $J =  \sum_{i=1}^k \sum_{x\in S_i} | x - \mu_i|^2$<br />
Take derivative:</p>

\[\frac{\partial J}{\partial \mu_i} 
        = \frac{\partial}{\partial \mu_i} \Big[ \sum_{x\in S_i} (x_i - \mu_i)^\top(x_i - \mu_i)\Big]
        = -2 \sum_{x\in S_i} (x_i - \mu_i)\]

<p>Set to 0:</p>

\[\sum_{x\in S_i} (x_i - \mu_i)
        = 0 \iff \sum_{x\in S_i} x_i = |S_i|\mu_i \iff \mu_i = \frac{1}{|S_i|}\sum_{x\in S_i} x_i\]

<p><strong>Initialization methods</strong></p>
<ul>
  <li>Random Partition: assign each point randomly to a cluster, compute means</li>
  <li>
    <p>Forgy Method: pick K random points as initial centroids</p>
  </li>
  <li><strong><em>K-means++</em></strong>: spreads out the initial cluster centers. Cluster centers are sampled sequentially and each new cluster is chosen from the remaining data points with probability proportional to its square distance from the closest existing cluster center.
    <blockquote>
      <ul>
        <li>$m_1 \sim \mathrm{Uniform}({x_1, x_2, \dots, x_n})$</li>
        <li>for $t = 2, \dots, k$:
          <ul>
            <li>for each point $x_i$:
              <ul>
                <li>$d(x_i) = \min_{j &lt; t} |x_i - m_j|^2$</li>
              </ul>
            </li>
            <li>$P(x_i \text{ selected}) = \frac{d(x_i)}{\sum_j d(x_j)}$</li>
          </ul>
        </li>
      </ul>
    </blockquote>
  </li>
  <li>Farthest Point: deterministic, always pick farthest point from current centroids</li>
</ul>

<p><strong>Variants</strong></p>
<ul>
  <li><em>K-medians</em>: minimize $\sum_{i=1}^k \sum_{x\in S_i} | x - \mu_i|$. More robust to outliers</li>
  <li><em>K-medoids</em>: has the constraint that $\mu_k$ must be an actual data point</li>
  <li><em>Fuzzy C-means</em>: soft clustering approach, minimize $\sum_{i=1}^k \sum_{j} w_{ji}^m || x_j - \mu_i||^2$ where each element $w_{ji}$ tells the degree to which element $x_j$ belongs to cluster $S_i$. $w_{ji} = \Big[\sum_{i=1}^k\big(\frac{ | x_j - \mu_i|}{| x_j - \mu_k|} \big)^{\frac{2}{m-1}}\big]^{-1} $</li>
  <li><em>Kernel K-means</em>: Operate in implicit feature space $\psi(x)$</li>
  <li><em>Spherical K-means</em>: use cosine similarity instead of euclidean. Normalize updates $\mu_k = (\Sigma x_i) / | \Sigma x_i |$</li>
</ul>

<p><strong>Computational upgrades</strong></p>
<ul>
  <li><em>Vectorization</em>: $|x_i - \mu_k|^2 = x_i^\top x_i - 2x_i^\top\mu_k + \mu_k^\top\mu_k$ which can be computed as matrix operations. So we can compute all pairwise squared distances efficiently and don’t need to loop over: observations, clusters, dimensions.</li>
  <li><em>Mini-Batch K-means</em>: $\mu_k^{new} =\mu_k + \frac{b}{v_k + b} \big( \frac{1}{b} \sum_{i=1}^b x_i - \mu_k\big)$ if we’ve seen $v_k$ points in cluster $k$ so far and we assign $b$ new points to that cluster.</li>
</ul>

<p><strong>Problems with K-Means</strong></p>
<ul>
  <li>Cluster of similar expected size. This is the assumption of spherical cluster that are separable so that the mean converges towards the geometric cluster center. So K-means works poorly on elongated and concentric clusters and clusters of different density.</li>
  <li>Can only find convex clusters.</li>
  <li>Sensitive to initialization. Different starting points can yield very different final clusters. So can converge to a local minimum.</li>
  <li>Sensitive to scale. Requires careful normalization/standardization otherwise features with large variance dominate the distance calculation.</li>
  <li>Assumes equal importance to each feature.</li>
  <li>Sensitive to outliers and noise.</li>
  <li>Computationally expensive: $O(n\times k\times d \times iterations)$.</li>
  <li>Curse of dimensionality: all pairwise distance becomes similar (concentration phenomenon).</li>
</ul>

<hr />
<h2 id="hierarchical-clustering">Hierarchical Clustering</h2>

<p>Hierarchical clustering provides an alternative view on K–means in the sense that it does not require us to pre-specify the number of clusters $K$. It results in a tree-based representation of the observations, called a <em>dendogram</em>. It also requires to specify a measure of (dis)similarity between <em>groups</em> of observations. Hierarchical clustering can be done in two manners: \texit{agglomerative} or <em>divisive</em>. Agglomerative recursively merge a selected pair of clusters into a single one, where the pair chosen consists of the two groups with the smallest pairwise dissimilarity. Divisive methods start with the full dataset and recursively split one cluster into two new.<br />
The extent to which the hierarchical structure produced by a dendogram actually fits the data can be judged by the <em>cophenetic correlation coefficient</em>, which is the correlation between all the $N(N-1)/2$ pairwise dissimilarities $d(i,j)$ and their cophenetic dissimilarities $C(i,j)$ produced by the dendogram.</p>

<!-- ![Diagram](/images/dendogram.jpg) -->

<p align="center">
<img src="/images/dendogram.jpg" data-light-src="/images/dendogram.jpg" data-dark-src="/images/dendogram_dark.png" alt="Diagram" />
</p>

<h3 id="cluster-linkage">Cluster Linkage</h3>
<ul>
  <li><strong><em>Maximum / Complete</em></strong> linkage: \(\max_{a\in A, b \in B} d(a,b)\)</li>
  <li><strong><em>Minimum / single</em></strong> linkage (nearest neighbour): \(\min_{a\in A, b \in B} d(a,b)\)</li>
  <li><strong><em>Average</em></strong> linkage: $\frac{1}{ | A | \cdot | B |} = \sum_A \sum_B d(a,b)$</li>
  <li><strong><em>Centroid</em></strong> linkage: \(d(\mu_A, \mu_B)\)</li>
  <li><strong><em>Ward’s Method</em></strong> (Minimum Variance): $\frac{|A|\cdot |B|}{|A|+|B|} || \mu_a - \mu_b||^2$</li>
</ul>

<hr />
<h2 id="choice-of-distance-functions">Choice of Distance functions</h2>
<ul>
  <li><strong><em>Minkowski Distance</em></strong>: $d(x,y) = \Big( \sum_i | x_i - _i|^q\Big)^{1/q} $
    <ul>
      <li><strong><em>Euclidean Distance</em></strong> for $q = 2$</li>
      <li><strong><em>Manhattan Distance</em></strong> for $q = 1$</li>
      <li><strong><em>Chebyshev Distance</em></strong> for $q \to \infty$</li>
    </ul>
  </li>
  <li><strong><em>Mahalanobis Distance</em></strong>: \(d(x,y) = \sqrt{(x-y)^\top \Sigma^{-1}(x-y)}\)
    <ul>
      <li>Covariance aware: computes ordinary Euclidean distance in the correlation and variance corrected space</li>
      <li>Requires estimation of covariance matrix so can be unstable in high dimensions, and expensive</li>
    </ul>
  </li>
  <li><strong><em>Cosine Distance</em></strong> ($1 -$ Cosine Similarity): \(d(x,y) = 1 - \frac{x \cdot y}{\|x\| \|y\|}\)
    <ul>
      <li>Excellent for high dimensional sparse vector</li>
    </ul>
  </li>
  <li><strong><em>Hamming distance</em></strong> (for <em>Categorical Data</em>): \(d(x,y) = \sum_i I(x_i \neq y_i)\)</li>
  <li><strong><em>Dynamic Time Warping</em></strong> (for <em>Time Series</em>):
    <ul>
      <li>First build a cost matrix where $d$ is usually the Euclidean distance: $C(t_1, t_2) = d(X_{t_1}, Y_{t_2})$</li>
      <li>Calculate cumulative path with Dynamic Programming:
  \(D(t_1, t_2) = C(t_1, t_2) + \min \Big\{ D(t_1-1, t_2), D(t_1, t_2-1), D(t_1-1, t_2-1)\Big\}\)</li>
      <li>
        <p>the <strong><em>DTW</em></strong> distance is the value for the final time indices. The path taken by the dynamic programming table is called the <strong><em>warping path</em></strong></p>

        <ul>
          <li>Works even when time series are misaligned, different speeds or unequal lenghts</li>
          <li>Very effective for clustering and classification</li>
          <li>Computation is $O(n \times m)$ where $n$ and $m$ are the length of each series, so can be slow.</li>
          <li>Not a <em>true</em> metric (as triangle inequality fails)</li>
        </ul>
      </li>
    </ul>
  </li>
</ul>

<hr />
<h2 id="metrics-and-number-of-clusters">Metrics and Number of Clusters</h2>

<h3 id="clustering-metrics">Clustering Metrics</h3>
<ul>
  <li><strong><em>WCSS / Inertia</em></strong> (Within-Cluster Sum of Squares): measures the total square distance from each point to its cluster center:
  \(WCSS = \sum_{i=1}^k \sum_{x\in S_i} \| x - \mu_i\|^2\)
  use it to compare different methods with the same number of clusters.</li>
  <li><strong><em>BCSS</em></strong> (Between-Cluster Sum of Squares)
  \(BCSS = \sum_{i\neq j}^k \|\mu_i - \mu_j \|^2\)</li>
  <li><strong><em>TSS</em></strong> (Total Sum of Squares) $TSS = WCSS + BCSS$</li>
  <li><strong><em>Silhouette Coefficient</em></strong> (<em>Most popular internal metric</em>): For each point $i$:
    <ul>
      <li>$a(i) = $ average distance from i to all other points in the same cluster</li>
      <li>$b(i) = $ <em>minimum</em> average distance from i to points in any other cluster</li>
    </ul>

\[Silhouette \hspace{0.5em} s(i) = \frac{b(i) - a(i)}{\max(a(i), b(i))}\]

    <ul>
      <li>$s(i) \approx 1$ tells that the point is well clustered (far from other points), $s(i) \approx 0$ that the point is on the boundary between clusters, and $s(i) \approx -1$ that the point is probably in the wrong cluster</li>
      <li>$Overall \hspace{0.5em} Silhouette \hspace{0.5em} Score = \frac{1}{n} \sum s(i)$</li>
      <li>Works with any distance metrics. However it has $O(n^2)$ computation, is biased towards complex clusters and can be sensitive to noise and outliers.</li>
      <li>Silhouette plot generally shows the histogram of silhouettes per cluster</li>
    </ul>
  </li>
  <li><strong><em>Davies-Bouldin Index</em></strong>:
      \(DB = \frac{1}{n} \sum_{i=1}^k \max_{i \neq j} \bigg( \frac{\sigma_i + \sigma_j}{d(\mu_i, \mu_j)}\bigg)\)
    <ul>
      <li>$\sigma_i$ is the average distance of all elements in cluster $i$ to its centroid $\mu_i$</li>
      <li>$d(\mu_i, \mu_j)$ is the distance between centroids</li>
      <li>The range is $[0, \infty)$ and $0$ is optimal</li>
      <li>$\max_{i \neq j} \big( \frac{\sigma_i + \sigma_j}{d(\mu_i, \mu_j)}\big)$ is the worst-case similarity of cluster $i$ to another cluster, and $DB$ is the average worst-case similarity across all clusters</li>
      <li>$O(nK)$, but biased towards spherical clusters, sensitive to outliers, and might not work well with different density clusters</li>
    </ul>
  </li>
  <li><strong><em>Dunn Index</em></strong>:
      \(D = \frac{\min_{1\leq i &lt; j \leq k} d(i,j)}{\max_{1 \leq c \leq k} d'(c)}\)
    <ul>
      <li>$d(i,j)$ is the distance between clusters $i$ and $j$, which can be any number of distance measure, such as the distance between the centroids of the clusters</li>
      <li>$d’(c)$ is a measure of intra-cluster distance for cluster $c$.  It can also be measured any metric, like the maximal distance between any two elements in cluster $c$</li>
      <li>The range is $[0, \infty)$ and higher is better</li>
      <li>$O(n^2)$ Complexity, extremely sensitive to outliers (numerator and denominator) and noise</li>
    </ul>
  </li>
</ul>

<h3 id="choosing-the-number-of-clusters">Choosing the number of Clusters</h3>
<ul>
  <li><strong><em>Elbow Method</em></strong>:
    <ul>
      <li>The Elbow method consists of choosing a range of $k$ values, and computing the $WCSS$ for each $k$, and plotting it. Look for elbow where the rate of decrease sharply drops</li>
      <li>Works as $WCSS$ always decreases with higher $k$</li>
      <li>However can have some problems, like no clear elbow or multiple elbows.</li>
      <li>One can use the <em>Kneedle Algorithm</em>, which is an automatic detection algorithm, or the <em>Second Derivative</em> method</li>
    </ul>
  </li>
  <li><strong><em>Silhouette Analysis</em></strong>: Can theoretically work with other metrics but <em>Silhouette</em> score is always the preferred one. Just plot Silhouette vs. number of clusters and pick the highest one.</li>
  <li><strong><em>Dendogram</em></strong>: Usually done for Hierarchical Clustering</li>
</ul>

<!-- ![Diagram](/images/elbow_silhouette.jpg) -->

<p align="center">
<img src="/images/elbow_silhouette.jpg" data-light-src="/images/elbow_silhouette.jpg" data-dark-src="/images/elbow_silhouette_dark.png" alt="Diagram" />
</p>

<p><em>Note: This is not supposed to represent plots for the same dataset / clustering method. Those are just illustrative. And yes, the right choice looks like $K=4$ for the Elbow plot, and $K=5$ for the Silhouette</em></p>

<hr />
<h2 id="gaussian-mixture-models">Gaussian Mixture Models</h2>
<p>GMM is a probabilistic model assuming the data is generated from a mixture of $K$ Gaussian distribution with unkownd parameters. The model takes the following form:
\(p(x) = \sum_{k=1}^K \pi_k \cdot \mathcal{N}(x |\mu_k, \Sigma_k)\)</p>
<ul>
  <li>Soft clustering: each point has a (posterior) probability of belonging to each cluster</li>
  <li>Flexible cluster shapes (can have different orientations, sizes)</li>
  <li>Assumes Gaussian (fails it data isn’t $\approx \mathcal{N})$</li>
  <li>Sensitive so initialization</li>
  <li>Requires to specify $K$ upfront</li>
  <li>Expensive in parameters and has requires a lot of data as can be unstable in high dimensions</li>
</ul>

<p><strong>Likelihood to maximize</strong>:</p>

\[L(\theta)
= \prod_{i=1}^n \sum_{k=1}^K \pi_k\, \mathcal{N}(x_i \mid \mu_k, \Sigma_k)
\qquad \iff \qquad
\ell(\theta)
= \sum_{i=1}^n \log\!\left( \sum_{k=1}^K \pi_k\, \mathcal{N}(x_i \mid \mu_k, \Sigma_k) \right)\]

<p>The presence of the logarithm of a sum prevents closed-form parameter updates, making direct maximization intractable.</p>

<p><strong>Posterior Probabilities (Soft Assignments)</strong>:<br />
Using Bayes’ rule, the responsibility of component $k$ for point $x_i$ is:</p>

\[\gamma_{ik}
= P(z_i = k \mid x_i, \theta)
= \frac{\pi_k\, \mathcal{N}(x_i \mid \mu_k, \Sigma_k)}
       {\sum_{j=1}^K \pi_j\, \mathcal{N}(x_i \mid \mu_j, \Sigma_j)}.\]

<p><strong>Complete-Data Log-Likelihood</strong>:<br />
If the latent assignment indicators $z_{ik}$ were known:</p>

\[\ell_c(\theta)
= \sum_{i=1}^n \sum_{k=1}^K
z_{ik}\,\big[ \log \pi_k + \log \mathcal{N}(x_i \mid \mu_k, \Sigma_k) \big].\]

<p>The <strong><em>EM</em></strong> algorithm optimizes the <em>expected</em> complete-data log-likelihood:</p>

<h3 id="expectation-maximization">Expectation Maximization</h3>
<p>The <strong><em>EM</em></strong> algo is an 2-step iterative optimization technique to estimat ethe unkown parameters in probabilistic models.</p>

<ul>
  <li>
    <p><strong><em>E-Step (Expectation Step)</em></strong>:</p>

\[\gamma_{ik}^{(t)}
  = P(z_i = k \mid x_i, \theta^{(t)})
  = \frac{\pi_k^{(t)}\, \mathcal{N}(x_i \mid \mu_k^{(t)}, \Sigma_k^{(t)})}
         {\sum_{j=1}^K \pi_j^{(t)}\, \mathcal{N}(x_i \mid \mu_j^{(t)}, \Sigma_j^{(t)})}.\]

    <ul>
      <li>Compute the posterior “responsibility” that component $k$ generated point $x_i$ by using the current parameter estimates $\theta^{(t)} = {\pi_k^{(t)},\mu_k^{(t)},\Sigma_k^{(t)}}$.</li>
      <li>Converts latent assignments $z_{ik}$ into soft probabilities $\gamma_{ik}$, i.e. expected values of the hidden variables $z_{ik}$.</li>
    </ul>
  </li>
  <li>
    <p><strong><em>M-Step (Maximization Step)</em></strong></p>

\[\pi_k^{(t+1)} = \frac{1}{n}\sum_{i=1}^n \gamma_{ik}^{(t)}, 
  \qquad
  \mu_k^{(t+1)} = \frac{\sum_{i=1}^n \gamma_{ik}^{(t)} x_i}{\sum_{i=1}^n \gamma_{ik}^{(t)}},\]

\[\Sigma_k^{(t+1)} = 
  \frac{\sum_{i=1}^n \gamma_{ik}^{(t)} (x_i - \mu_k^{(t+1)})(x_i - \mu_k^{(t+1)})^\top}
       {\sum_{i=1}^n \gamma_{ik}^{(t)}}\]

    <ul>
      <li>Update mixture weights, means, and covariance matrices using $\gamma_{ik}^{(t)}$, assuming those the soft assignments are correct. These are basically the Maximum Likelihood Estimates of the parameters, given the soft-assignments.</li>
      <li>Maximizes the expected complete-data log-likelihood:
      $
      Q(\theta \mid \theta^{(t)}) = 
      \mathbb{E}_{z|X,\theta^{(t)}}[\ell_c(\theta)].
      $</li>
    </ul>
  </li>
</ul>

<p>When I was an undergraduate student, I always found the Expectation-Maximization algorithm very unintuitive. I learned it many times, but it never stuck, because I never truly understood it. I’m still no pro of that EM procedure, but maybe this will help an unfortunate student that wished his exam was on supervised learning only.</p>

<p>It turns out that <strong><em>LLoyd’s Algorithm</em></strong> for K-means is a special case of the <strong><em>Expectation Maximization</em></strong> procedure. Actually, under certain (very strict) conditions, the two are equivalent. You would essentially need each component to have equal, spherical covariance, the variances going to 0, each cluster having equal mixing weight (i.e. equal $\pi_k$), and the procedure itself to be a hard assignment method instead of a soft, probabilistic algorithm. This doesn’t help much does it?</p>

<p>What if I told you the <strong><em>E-Step</em></strong> was equivalent to the <strong><em>Assignment Step</em></strong> and the <strong><em>M-Step</em></strong> to the <strong><em>Update Step</em></strong>? One could see that the E-step calculates the expected membership probabilities for (or associates the cluster center to) each data point under the current parameter estimates (or centroids positions), and the M-Step updates the model parameters (centroid positions) given the current membership probabilities (cluster assignments).</p>

<p><strong>Convergence</strong></p>
<ul>
  <li>The observed (expected) log-likelihood increases monotonically (actually non-decreasing).</li>
  <li>EM converges to a stationary point, however it might be a saddle point or a local maximum</li>
</ul>

<p><br />
 <!-- \ --></p>

<p>This is all for today. Actually, this is way too much for any day.</p>

<hr />
<h2 id="references">References</h2>
<p><strong>The Elements ofStatistical Learning: Data Mining, Inference, and Prediction.</strong> (2009)<br />
Trevor Hastie, Robert Tibshirani, Jerome Friedman<br />
<a href="https://hastie.su.domains/ElemStatLearn/" target="_blank">Book</a></p>

<p><strong>k-means++: The Advantages of Careful Seeding</strong> (2006)<br />
David Arthur, Sergei Vassilvitskii<br />
<a href="http://ilpubs.stanford.edu:8090/778/1/2006-13.pdf" target="_blank">Paper</a></p>

<p><strong>Maximum Likelihood from Incomplete Data via the EM Algorithm</strong> (1977)<br />
A. P. Dempster, N. M. Laird, D. B. Rubin<br />
<a href="https://www.eng.auburn.edu/~roppeth/courses/00sum13/7970%202013A%20ADvMobRob%20sp13/literature/paper%20W%20refs/dempster%20EM%201977.pdf" target="_blank">Paper</a></p>

<p><strong>The EM Algorithm and Extensions</strong> (2008)<br />
GJ McLachlan, T Krishnan<br />
<a href="https://books.google.fr/books?hl=en&amp;lr=&amp;id=NBawzaWoWa8C&amp;oi=fnd&amp;pg=PR3&amp;dq=+Geoffrey+McLachlan+and+Thriyambakam+Krishnan.+The+EM+Algorithm+and+Extensions&amp;ots=tqc6TR_yvR&amp;sig=XzSWL-iHA0chDfZJ2TfqHSp0LAM&amp;redir_esc=y#v=onepage&amp;q=Geoffrey%20McLachlan%20and%20Thriyambakam%20Krishnan.%20The%20EM%20Algorithm%20and%20Extensions&amp;f=false" target="_blank">Book</a></p>]]></content><author><name></name></author><summary type="html"><![CDATA[K-means]]></summary></entry><entry><title type="html">The Sharpe Ratio</title><link href="http://localhost:4000/blog/Sharpe/" rel="alternate" type="text/html" title="The Sharpe Ratio" /><published>2000-01-03T00:00:00+00:00</published><updated>2000-01-03T00:00:00+00:00</updated><id>http://localhost:4000/blog/Sharpe</id><content type="html" xml:base="http://localhost:4000/blog/Sharpe/"><![CDATA[\[\begin{align*}
SR &amp;= \frac{E[R]}{\sigma}  \quad \quad \quad \text{with} \quad 
E[R] = \frac{1}{m} \sum R_i \quad\text{and} \quad 
\sigma_R = \frac{1}{n-1} \sqrt{\sum(R_i - E[R])^2} \quad \quad

\end{align*}\]

<p>Assuming returns are normally distributed.</p>

<p>Well, traditionally, we do $SR= \frac{E[R] - r}{\sigma}$. But close enough. The truth is, the Sharpe Ratio is more of a means of comparing strategies of similar capacity rather than being a standalone metric. Actually, without the contex of the trading frequency and the capacity, a Sharpe Ratio by itself is pretty uninformative. A Sharpe Ratio of 2 for ultra-high-frequency strategy is quite unimpressive, while it is extremely rare for a fundamental, long-term investor to maintain this performance for a significant amount of time.</p>

<p>It turns out that the limiting distribution of the Sharpe Ratio is Gaussian. Actually, this is the case even if returns are non-Normal. You can thank the CLT and the asymptotic distribution of the MLE for that.</p>

\[\begin{equation*}
\text{we have } \sqrt{n} (\hat{SR} - SR) \Longrightarrow N(0, 1+ \frac{SR^2}{2})
\end{equation*}\]

<hr />
<h2 id="derivation-of-the-variance-of-the-sharpe-ratio">Derivation of the Variance of the Sharpe Ratio</h2>

<p>Assume Returns $R_t \overset{\text{iid}}{\sim} N(\mu, \sigma^2)$<br />
Our parameters vector is $\theta = (\mu, \sigma^2)^T$</p>

<p>From the <strong>Asymptotic normality of the MLE</strong>, we have
\(\sqrt{m} (\hat{\theta} - \theta) \xrightarrow{d} N(0, V)\) with
\(\begin{equation*}
V = \begin{pmatrix}
\sigma^2 &amp; 0 \\
0 &amp; 2\sigma^4
\end{pmatrix} = I^{-1}
\end{equation*}\)</p>

<p>Here, $I$ is the Fisher Information matrix, defined as
\(I = - E \left[ \frac{\partial^2 \ell(\theta)}{\partial \theta^2} \right]\)</p>

<p>Then the <strong>MLE</strong> is:
\(\hat{\theta} = \begin{pmatrix}
\frac{1}{n} \sum R_i \\
\frac{1}{n} \sum (R_i - \bar{R})^2
\end{pmatrix}\)</p>

<p>The <strong>Delta Method</strong> gives:
\(\sqrt{n} (g(\hat{\theta}) - g(\theta)) \xrightarrow{d} N(0, \frac{\partial g(\theta)}{\partial \theta}^T V \frac{\partial g(\theta)}{\partial \theta})\)
where $\frac{\partial g(\theta)}{\partial \theta}$ is the Jacobian matrix = 
\(\begin{pmatrix}
\frac{1}{\sigma} \\
-\frac{\mu}{2\sigma^3}
\end{pmatrix}\)</p>

<p>Hence we have 
\(\frac{\partial g(\theta)^T}{\partial \theta} V \frac{\partial g(\theta)}{\partial \theta} = \begin{pmatrix}
\frac{1}{\sigma} &amp;
-\frac{\mu}{2\sigma^3}
\end{pmatrix} \begin{pmatrix}
\sigma^2 &amp; 0 \\
0 &amp; 2\sigma^4
\end{pmatrix} \begin{pmatrix}
\frac{1}{\sigma} \\
-\frac{\mu}{2\sigma^3}
\end{pmatrix} = \begin{pmatrix}
\sigma &amp;
-\mu\sigma
\end{pmatrix}\begin{pmatrix}
\frac{1}{\sigma} \\
-\frac{\mu}{2\sigma^3}
\end{pmatrix} = 1+ \frac{\mu^2\sigma}{2\sigma^3} = 1 + \frac{1}{2} SR^2\)</p>

<hr />

<h3 id="relationship-to-t-stat">Relationship to $t$-stat</h3>

<p>You want to test whether the returns of your strategy are statistically significant (different from 0 really). If you regress $R_t \sim 1$ (i.e. predicting the returns using only an intercept), you can test whether the hypothesis test $\beta_0 = 0$ (the intercept) using the t-statistic from OLS.
Recall \(t = \frac{\hat{\beta}}{SE(\hat{\beta})}\)
We have:</p>
<ul>
  <li>$\hat{\beta} = \mathbb{E}[R_t]$</li>
  <li>$SE(\hat{\beta}) = \sigma \sqrt{(X^TX)^{-1}}$, with</li>
  <li>$\sigma = \frac{1}{\sqrt{n-1}} \sqrt{\Sigma(R_t - \beta_0)^2} = \frac{1}{\sqrt{n-1}}\sqrt{\Sigma(R_t - \mathbb{E}[R_t])^2} = \sigma_R$</li>
  <li>$\sqrt{(X^TX)^{-1}} = \sqrt{\Sigma 1} = \sqrt{n}$</li>
</ul>

<p>So we get</p>

\[t = \frac{\hat{\beta}}{SE(\hat{\beta})}
    = \frac{\mathbb{E}[R_t]}{\sqrt{n} \cdot \sigma_R} = \frac{1}{\sqrt{n}} \cdot SR\]

<p>So we can see that the Sharpe Ratio is equivalent to the t-statistic for $\beta_0$ up to a scaling constant.</p>

<hr />
<h3 id="time-aggregation">Time Aggregation</h3>
<p>When we talk about Sharpe Ratio we generally talk about the <em>Annualized</em> Sharpe Ratio, which would be the SR measured on yearly returns. But we can transform the SR measured w.r.t shorter time periods t into a SR measured w.r.t longer time periods T (assuming t divides T), let $q = T/t$.</p>

<p>We know that \(R_T = \sum^q_i R_t^{(i)}\)</p>

<p>Under the assumption of i.i.d returns $R_t$,</p>

\[\mathbb{E}[R_T] = q \cdot \mathbb{E}[R_t] \qquad \text{ and } \qquad Var[R_T] = q\cdot Var[R_t]\]

\[SR[R_T] = \frac{q \cdot \mathbb{E}[R_t]}{\sqrt{q\cdot Var[R_t]}} = \sqrt{q}\cdot SR[R_t]\]

<hr />

<h3 id="sharpe-ratio-under-non-normality">Sharpe Ratio under non-normality</h3>
<p>We have shown that under normality conditions $Var(SR) = 1+ \frac{SR^2}{2}$. If we drop the normality conditions fo the returns, we can show that:</p>

\[Var(SR) = 1+ \frac{SR^2}{2} - SR \cdot \gamma_3 + SR^2 \cdot \frac{\kappa - 3}{4}\]

<p>where $\gamma_3$ is the skew of returns and $\kappa$ the kurtosis. There are several consequences of using the <strong>standard variance</strong> formula instead of the true one under non-normality of returns:</p>
<ul>
  <li>$\gamma_3 &gt; 0$ makes the standard variance $Var(SR) = 1+ \frac{SR^2}{2}$ inflated where as the true variance would be lower. This makes the SR look less significant. Opposedly, a negative skew $\gamma_3 &lt; 0$ will make the standard SR look more significant.</li>
  <li>Similarly, high kurtosis $\kappa&gt;3$ makes the standard variance formula lower than it really is. This makes the SR look better.</li>
</ul>

<hr />
<h3 id="combining-portfolios">Combining Portfolios</h3>

<p>I will do something that is generally well-appreciated in the mathematical literature and change notations. So far we denoted the Sharpe Ratio as $SR$. Now I’m changing to $S$. And $P$ denotes a portfolio. It looks like that:</p>

\[\begin{equation*}
P = \sum w_i X_i
\end{equation*}\]

<p>Let $C$ be the covariance matrix of $X$ : $C_{ij} = \text{cov}(x_i, x_j)$</p>

\[\begin{align*}
E[P] &amp;= \sum w_i E[P_i] \\
\text{Var}(P) &amp;= \sum\sum w_i w_j \text{cov}(X_i, X_j) = w^T C w
\end{align*}\]

<h4 id="independent-strategies">Independent strategies:</h4>

<p>Let $S_i = \frac{E[X_i]}{\sigma_i}$</p>

<p>Assume strategies are $iid$, so  $S_i = S_j = S$.</p>

\[\begin{align*}
\text{Var}(P) = \sigma_P^2 &amp;= \frac{1}{n^2} \sum \sigma^2 = \frac{\sigma^2}{n}\\
\sigma_P &amp;= \frac{\sigma}{\sqrt{n}}\\
E(P) &amp;= \frac{1}{n} \sum E[X_i] = \frac{1}{n} \sum S_i \sigma_i\\
\\
S_P &amp;=\frac{E(P)}{\sigma_P} =  \frac{\frac{1}{n}\sum S_i \sigma_i}{\frac{\sigma}{\sqrt{n}}} = \frac{S\sigma}{\frac{\sigma}{\sqrt{n}}} = \sqrt{n} S
\end{align*}\]

<p>So you can see that the Sharpe Ratio scales <strong>at best</strong> with the square root of number of strategies.</p>

<h4 id="correlated-strategies">Correlated strategies:</h4>

<p>Here we assume that the strategies all share the same correlation $\text{Corr}(X_i, X_j) = \rho$. We still have \(E[P] = \frac{1}{n} \sum S_i \sigma_i\).</p>

\[\begin{align*}
\sigma_P^2 &amp;= \text{Var}(\frac{1}{n} \sum X_i) = \frac{1}{n^2} \sum \text{Var}(X_i) + \frac{1}{n^2} \sum_{i \neq j} \text{Cov}(X_i, X_j) \\
&amp;= \frac{1}{n^2} [n \sigma^2 + n(n-1) \rho \sigma^2] = \frac{\sigma^2}{n} [1+(n-1)\rho] = \frac{\sigma^2}{n} [1+(n-1)\rho]
\end{align*}\]

<p>So</p>

\[S_P = \frac{E(P)}{\sigma_P} = \frac{\frac{1}{n} \sum S_i \sigma_i}{\frac{\sigma}{\sqrt{n}} \sqrt{1+(n-1)\rho}} = \frac{\sqrt{n}}{\sqrt{1+(n-1)\rho}}S\]

<h4 id="number-of-effective-strategies-in-the-correlated-case">Number of effective strategies in the correlated case:</h4>

<p>If we had $N_{eff}$ independent strategies: $\sigma_P^{eff} = \frac{\sigma}{\sqrt{N_{eff}}}$</p>

<p>So,
\(\frac{\sigma}{\sqrt{N_{eff}}} = \frac{\sigma}{\sqrt{n}} \sqrt{1+(n-1)\rho}\)
\(\Rightarrow N_{eff} = \frac{n}{1+(n-1)\rho}\)</p>

<!-- ![Diagram](/images/CombinedSharpe.jpg) -->

<p align="center">
<img src="/images/CombinedSharpe.jpg" data-light-src="/images/CombinedSharpe.jpg" data-dark-src="/images/CombinedSharpe-dark3.png" alt="Combined Sharpe Ratio Evolution" />
</p>

<p>The plot above shows the evolution of the combined Sharpe of x strategies all with individual Sharpe Ratio of 1. One can easily see how correlation within strategies hinder the global performance of the combined portfolio. Scalability of a hedge fund depends mostly on how uncorrelated the individual strategies are. This is why Millenium pods cannot talk to each other, by the way — to make sure that alpha doesn’t leak from one portfolio to another.</p>

<h3 id="drawdowns-advanced">Drawdowns (advanced)</h3>
<p>If you’re wondering how I derived that, I didn’t. I stole it. From Gappy. This is slightly advanced, and I have not used it in practice. But upon reading it, I thought I needed to make a note, so here it is. I certainly hope that my next interviewer for a QR position will not find this blog and ask about it, otherwise I’m cooked. Not even sure that this has its place on this blog. This is not even Sharpe Ratio anymore.</p>

<p>Consider the PnL of a strategy as a diffusion $X_t$ with drift $\mu &gt; 0$ and volatility $\sigma$. We can interpret $\mu$ as the expected daily PnL and $\sigma$ as the daily vol.
\(dX_t = \mu dt + \sigma dB_t\)
\(X_0 = 0\)</p>

<p>Define the Sharpe $SR_T = \frac{\sqrt{T} \mu}{\sigma}$ and $\sigma_T = \sigma \sqrt{T}$ for a given horizon $T$. When considering a drawdown $-b$, we also define the <em>normalized drawdown</em> $b_{\sigma} = b/\sigma$</p>

<ul>
  <li><strong>DD probability:</strong> $P(b) = P(X_t  \leq b \text{  for some  } t&gt;0)$
  \(P(b) = \exp \Bigl\{ - \frac{2 \mu b}{\sigma^2}\Bigl\} = \exp ( -2 b_\sigma SR)\)</li>
  <li>
    <p><strong>Stationary distribution of DD</strong> $D_t = max_{0\leq s\leq t} X_s - X_t$
  \(P(D_t&gt;=b) \rightarrow^{t \rightarrow \infty} \exp \Bigl\{ - \frac{2 \mu b}{\sigma^2}\Bigl\} = \exp ( -2 b_\sigma SR)\)</p>

    <p>It follows that the average drawdown is \(E(D_t) = \frac{\sigma^2}{2 \mu} \Leftrightarrow \frac{E(D_t)}{\sigma} = \frac{1}{2SR}\)</p>
  </li>
  <li>
    <p><strong>Expected recovery time</strong>. Let \(D(t^{*}) = -b &lt;0\) meaning that at time \(t^*\), the process \(X_{t^*}\) is \(b\) below its prior maximum. Define</p>

\[\tau(b) = \inf \{ t \geq 0: D_{t^* + t} = 0 | D_{t^*} = -b\}\]

    <p>The expected time to recover is:</p>

\[E(\tau) = \frac{b}{\mu} = \frac{b_{\sigma}}{SR}\]

    <p>One can also show</p>

\[\text{std}(\tau) = \frac{b \sigma^2}{ \mu^3} = \sqrt{\frac{b_\sigma}{SR}}\frac{1}{SR}\]

    <p>To estimate the expected time spent in a DD, we can integrate the expected time to exit $-b$ w.r.t its stationary distribution defined above</p>

\[\begin{align*}
  \qquad \qquad \qquad \qquad \qquad \int_0^{\infty} \frac{2\mu}{\sigma^2} \frac{b}{\mu} \exp \Bigl\{ - \frac{2 \mu b}{\sigma^2}\Bigl\} db &amp;= \frac{2}{\sigma^2} \int b \exp \{-kb\} \qquad \text{with}\quad k=2\mu /\sigma^2\\
  &amp;= \frac{2}{\sigma^2}\frac{1}{k^2}\\
  &amp;= \frac{1}{2SR^2}  
  \end{align*}\]
  </li>
</ul>

<hr />
<h2 id="references">References</h2>
<p><strong>Sharpe Ratio: Estimation, Confidence Intervals, and Hypothesis Testing</strong> (2018)<br />
Matteo Riondato; Labs, Two Sigma Investments, LP<br />
<a href="https://www.twosigma.com/wp-content/uploads/sharpe-tr-1.pdf" target="_blank">Paper</a></p>

<p><strong>Comments on the Variance of the IID estimator in Lo (2002)</strong> (2002)<br />
Elmar Mertens<br />
<a href="https://drive.google.com/file/u/0/d/1LKSidazx6ZOtqlNQhL9j41mQ1GqubA7L/view?usp=sharing&amp;pli=1" target="_blank">Paper</a></p>

<p><strong>Reading, Writing and Drawdown Arithmetic</strong> (2025)<br />
Giuseppe Paleologo<br />
<a href="https://byfire.substack.com?utm_source=navbar&amp;utm_medium=web" target="_blank">Article</a></p>]]></content><author><name></name></author><summary type="html"><![CDATA[$$ \begin{align*} SR &amp;= \frac{E[R]}{\sigma} \quad \quad \quad \text{with} \quad E[R] = \frac{1}{m} \sum R_i \quad\text{and} \quad \sigma_R = \frac{1}{n-1} \sqrt{\sum(R_i - E[R])^2} \quad \quad]]></summary></entry><entry><title type="html">Regularization in Regression</title><link href="http://localhost:4000/blog/Shrinkage/" rel="alternate" type="text/html" title="Regularization in Regression" /><published>2000-01-02T00:00:00+00:00</published><updated>2000-01-02T00:00:00+00:00</updated><id>http://localhost:4000/blog/Shrinkage</id><content type="html" xml:base="http://localhost:4000/blog/Shrinkage/"><![CDATA[<h2 id="the-problem">The Problem</h2>
<p>Shrinkage methods are used for two reasons: to deal with <em>multicollinearity</em> and do <em>variable selection</em>. When high multicollinearity is present, the design matrix $X^TX$ becomes degenerate. When perfect multicollinearity is present, $rank(X^TX) &lt; n$ and the matrix is singular and thus non-invertible, so it is impossible to solve OLS. As a motivating example, perform the eigendecomposition of the design matrix:</p>

\[X^TX = Q\Lambda Q^{-1}\]

<p>Here $Q$ is a $n\times n$ matrix whose $i$th column is the $i$th eigenvector of $X^TX$. $\Lambda$ is a diagonal matrix of the eigenvalues of $X^TX$. In the case of <strong>non</strong>-perfect multicollinearity, i.e. when no eigenvalues are 0, then $X^TX$ is invertible and its inverse is given by:</p>

\[(X^TX)^{-1} = Q\Lambda^{-1} Q^{-1}\]

<p>where $\Lambda^{-1}$ is the diagonal matrix with inverse eigenvalues, e.g. $1/\lambda_i$. One can easily see that if $X^TX$ is close to singular then some if its eigenvalues will be close to 0, making exploding $\beta$.</p>

<h2 id="ridge">Ridge</h2>
<h3 id="general-form">General Form</h3>
<p>Instead of minimizing $|y-X\beta|^2$ (MSE) we now minimize $|y-X\beta|^2 + \lambda |\beta|^2$. This can also be formulated as</p>

\[\textit{minimize} \quad RSS \quad \text{subject to} \quad \Sigma\beta^2&lt;t\]

\[\hat{\beta}{_{RIDGE} = (X^TX + \lambda I)^{-1}X^Ty}\]

<p>The term $\lambda |\beta|^2$ is called the <em>shrinkage penalty</em>. When it is 0 we fall back to OLS. The $\hat{\beta}$ in OLS are scale invariant, multiplying $X_i$ by $c$ gives a new $\hat{\beta}_i$ scaled by $1/c$. So we must <strong>normalize</strong> the predictors before applying any shrinkage method.
Ridge regression’s advantage over least squares is rooted in the bias-variance trade-off. As $\lambda$ increases, the flexibility of the ridge regression fit decreases, leading to decreased variance but increased bias. We can see that in the case where $X^TX$ is singular, the additional diagonal term $\lambda I$ will push eigenvalues slightly upward, forcing it ot be non-singular. This is especially useful when $p &gt; n$.</p>
<h3 id="as-svd">As SVD</h3>
<p>Setting $X = UDV^T$ with $U$ and $D$ being unitary matrix (orthogonal if $X$ is real).\ 
The columns of $V$ are the <em>right-singular vectors</em> and eigenvectors of $X^TX$ and the columns of $U$ are the <em>left-singular vectors</em> and eigenvectors of $XX^T$. The non-zero elements of D are the <em>singular values</em>, i.e. the square roots of the eigenvalues of $X^TX$ (or $XX^T$). But you already knew that, right? Right?</p>

\[\begin{align*}
\hat{\beta} &amp;= (X^T X)^{-1} X^T Y\\
&amp;= (V D U^T U D V^T)^{-1} V D U^T Y\\
&amp;= (V D^2 V^T)^{-1} V D U^T Y\\
&amp;= V D^{-2} V^T V D U^T Y\\
&amp;= V D^{-1} U^T Y
\end{align*}\]

\[\begin{align*}
\hat{\beta}_{RIDGE} &amp;= (X^T X + \lambda I_p)^{-1} X^T Y\\
% &amp;= (V D U^T U D V^T + \lambda I_p)^{-1} V D U^T \\
&amp;= (V D^2 V^T + \lambda I_p)^{-1} V D U^T Y\\
&amp;= (V (D^2 + \lambda V^T V))^{-1} V D U^T Y\\
&amp;= V (D^2 + \lambda I_n)^{-1} V^T V D U^T Y\\
&amp;= V (D^2 + \lambda I_n)^{-1} D U^T Y\\
\end{align*}\]

<p>The ridge estimates are essentially the OLS estimates $\hat{\beta}=V D^{-1} U^T Y=V (D^2)^{-1}D U^T Y$ multiplied by the term $\frac{D^2}{D^2 + \lambda I_n}$, which is always between 0 and 1. This has the effect of shifting the coefficient estimates downward. The coefficients with a smaller corresponding value $d_i$ (the $i$th diagonal of $D$) will be whrunk more than coefficients with a large $d_i$. So covariates that account for very little of the variance in the data will be shifted to zero more quickly.</p>

<hr />
<h2 id="lasso">Lasso</h2>
<h3 id="general-form-1">General Form</h3>
<p>Lasso differs from Ridge by minimizing the L1 norm of the $\beta$ coefficients instead of L2: we know minimize $|y-X\beta|^2 + \lambda |\beta|$. The Lasso does not have a closed-form solution as you cannot directly differentiate this expression w.r.t $\beta$ due to the absolute value norm. This can also be formulated as</p>

\[\textit{minimize} \quad RSS \quad \text{subject to} \quad \Sigma|\beta|&lt;t\]

<p>Lasso has the property that it can set coefficients $\beta_j$ directly to 0. This can be interpreted through the <strong>subgradient conditions</strong>:</p>

\[\begin{align*}
L(\beta) &amp;= \frac{1}{2}\|y-X\beta\|^2 + \lambda |\beta|\\
\partial L(\beta) &amp;= X^T(X\beta - y) + \lambda\partial |\beta| \quad \text{(full gradient on first term)}
\end{align*}\]

<p>The optimality condition says that $\hat{\beta}$ minimizes $L$ iff $0 \in \partial L(\hat{\beta})$. For coefficient $j$, this becomes: \(0 = X_j^T(X\hat{\beta} - y) + \lambda s_j \quad \text{where } s_j \in \partial \|\hat{\beta}_j\|\)</p>

<p><strong>Case 1:</strong> If $\hat{\beta_j}= 0$</p>

<p>Then $s_j \in [-1, 1]$, so we need:</p>

\[\begin{align*}
0 &amp;= X_j^T(X\hat{\beta} - y) + \lambda s_j \\
s_j &amp;= -\frac{1}{\lambda}X_j^T(X\hat{\beta} - y)
\end{align*}\]

<p>This is valid only if \(\|s_j\| \leq 1\), which means:</p>

\[\begin{equation*}
\left|X_j^T(X\hat{\beta} - y)\right| \leq \lambda
\end{equation*}\]

<p>Therefore, $\hat{\beta}_j = 0$ is optimal if and only if the correlation between the residual and feature $j$ is less than $\lambda$.</p>

<p><strong>Case 2:</strong> If $\hat{\beta_j} \neq 0$</p>

<p>Then $s_j = \text{sign}(\hat{\beta}_j)$, so:</p>

\[\begin{equation*}
X_j^T(X\hat{\beta} - y) = -\lambda \cdot \text{sign}(\hat{\beta}_j)
\end{equation*}\]

<p>The subgradient is constant ($\pm\lambda$) regardless of the magnitude of $\beta_j$. This creates a constant push toward zero of size $\lambda$, which drives coefficients smaller than $\lambda$ to 0.
Ridge regression ($\lambda|\beta|^2$), whose gradient $2\lambda\beta_j$ is proportional to the current value—large coefficients get large shrinkage, small ones get small shrinkage, asymptotically approaching but never reaching zero.</p>

<hr />
<h2 id="bayesian-interpretation">Bayesian Interpretation</h2>
<p>We can explain Ridge and Lasso through a Bayesian interpretation”
\(\mathbb{P}(\beta|X,Y) \propto \mathcal{L}(Y|X, \beta) \cdot \mathbb{P}(\beta)\)
with $\mathbb{P}(\beta|X,Y)$ being the posterior distribution of $\beta$ given the data, $\mathcal{L}(Y|X, \beta)$ the likelihood and $\mathbb{P}(\beta)$ our prior on the distribution of $\beta$.<br />
Given $Y|X, \beta \sim N(X\beta,\sigma^2I)$</p>

<p>\(\mathcal{L}(Y|X, \beta) = \Pi \frac{1}{\sqrt{2\pi\sigma^2}} \exp{\biggl\{ -\frac{(y-X\beta)^2}{2\sigma^2}\biggl\}} = \Big( \frac{1}{\sqrt{2\pi\sigma^2}}\Big)^n \exp{\biggl\{-\frac{1}{2\sigma^2}\Sigma \varepsilon_i^2\biggl\}}\)</p>
<h3 id="ridge-1">Ridge</h3>
<p><strong>Assumes Gaussian prior:</strong> $\beta \sim N(0, \tau^2)$
\(\begin{align*}
    \hat{\beta}_{RIDGE} = argmax \hspace{.4em} \mathbb{P}(\beta|X,Y) &amp;= 
    argmax \hspace{.4em}  \Bigg\{
   \Big( \frac{1}{\sqrt{2\pi\sigma^2}}\Big)^n \exp{\biggl\{-\frac{1}{2\sigma^2}\Sigma \varepsilon_i^2\biggl\}} \cdot
   \Big( \frac{1}{\sqrt{2\pi\tau^2}}\Big)^p \exp{\biggl\{-\frac{1}{2\tau^2}\Sigma \beta_i^2\biggl\}}
   \Bigg\}\\
   &amp;= argmax \hspace{.4em}  \Bigg\{ \exp{\biggl\{-\frac{1}{2\sigma^2}\Sigma \varepsilon_i^2 -\frac{1}{2\tau^2}\Sigma \beta_i^2\biggl\}}
   \Bigg\}\\ 
    &amp;= argmin \hspace{.4em}  \bigg\{ \frac{1}{2\sigma^2}\Sigma \varepsilon_i^2 +\frac{1}{2\tau^2}\Sigma \beta_i^2\biggl\}
   \bigg\}\\ 
   &amp;= argmin \hspace{.4em}  \bigg\{ RSS +\frac{\sigma^2}{\tau^2}\Sigma \beta_i^2\biggl\} \qquad \qquad \text{which is RIDGE with $\lambda = \frac{\sigma^2}{\tau^2}$} \\  
\end{align*}\)</p>
<h3 id="lasso-1">Lasso</h3>
<p><strong>Assumes Laplace / Double exponential prior:</strong> \(\beta \sim \frac{1}{\sqrt{2b}} \exp{\big\{ - \frac{\Sigma |\beta|}{b}\big\}}\)
\(\begin{align*}
    \hat{\beta}_{RIDGE} = argmax \hspace{.4em} \mathbb{P}(\beta|X,Y) &amp;= 
    argmax \hspace{.4em}  \Bigg\{
   \Big( \frac{1}{\sqrt{2\pi\sigma^2}}\Big)^n \exp{\biggl\{-\frac{1}{2\sigma^2}\Sigma \varepsilon_i^2\biggl\}} \cdot
   \Big( \frac{1}{\sqrt{2b}}\Big)^p \exp{\biggl\{-\frac{1}{b}\Sigma |\beta_i|\biggl\}}
   \Bigg\}\\
   &amp;= argmax \hspace{.4em}  \Bigg\{ \exp{\biggl\{-\frac{1}{2\sigma^2}\Sigma \varepsilon_i^2 -\frac{1}{b}\Sigma |\beta_i|\biggl\}}
   \Bigg\}\\ 
    &amp;= argmin \hspace{.4em}  \bigg\{ \frac{1}{2\sigma^2}\Sigma \varepsilon_i^2 +\frac{1}{b}\Sigma |\beta_i|\biggl\}
   \bigg\}\\ 
   &amp;= argmin \hspace{.4em}  \bigg\{ RSS +\frac{2\sigma^2}{b}\Sigma |\beta_i|\biggl\} \qquad \qquad \text{which is LASSO with $\lambda = \frac{2\sigma^2}{b}$} \\  
\end{align*}\)</p>

<p>In this view, Lasso and Ridge are Bayes estimates with different priors. They are derived as posterior modes, that is, maximizers of the posterior. It is more common to use the mean of the posterior as the Bayes estimate. Ridge regression is also the posterior mean, but the Lasso is not.</p>

<!-- ![Diagram](/images/Gaussian_Laplace_grey.png) -->

<p align="center">
<img src="/images/Gaussian_Laplace_grey.png" data-light-src="/images/Gaussian_Laplace_grey.png" data-dark-src="/images/Gaussian_Laplace_dark.png" alt="Diagram" />
</p>

<p>One can easily see how the definitions of regularization as priors on the distribution of the coefficients relate to their shrinking behavior. Imagine your OLS solution $\hat{\beta}$ as a point on one of the two prior curves (Gaussian for Ridge, Laplace for Lasso), and the shrinking process as the effect of these priors “pulling” the estimate toward 0. Because the Laplace prior has a sharp peak at $0$, increasing the regularization coefficient $\lambda$ can pull a coefficient exactly to $0$. In contrast, the Gaussian prior is smooth at $0$, so the corresponding Ridge penalty only shrinks coefficients continuously toward $0$ and never forces them to be exactly zero.</p>

<h2 id="elastic-net">Elastic-Net</h2>
<p>In Ridge, we minimize $RSS + \lambda |\beta|^2$, and in Lasso, $RSS + \lambda |\beta|$. Elastic-net introduces a compromise, that has both selects variables like Lasso, and shrinks together the coefficients of correlated predictors like Ridge:</p>

\[\textit{minimize} \qquad RSS + \lambda \Sigma (\alpha \beta_j^2 + (1-\alpha)|\beta_j|)\]

<p>This introduces an extra parameter $\alpha$ that defines the strength of the L2-Norm relative to the L1-Norm.</p>

<p>You can compute the solution using the LARS-EN algorithm for the same computational cost as Lasso.</p>

<hr />
<h2 id="lasso-vs-ridge">Lasso vs Ridge</h2>

<h3 id="rotational-invariance">Rotational Invariance</h3>

<p>Ridge is <strong>Rotationally Invariant</strong> (e.g., the learning procedure and evaluation is unchanged when applying a rotation to the features on both the training and testing set. Intuitively, this mixes informative and uninformative signals. So to remove uninformative features, a rotationally invariant algorithm has to first find the original orientation of the features).</p>

<p><strong>Rotational Invariance (Ng, 2004):</strong></p>

<p>Let $\mathcal{M} = {M \in \mathbb{R}^{n \times n} : MM^T = M^TM = I, |M| = 1}$ be the class of rotation matrices.</p>

<p>A learning algorithm $\mathcal{L}$ is <strong>rotationally invariant</strong> if, for any training set $S$, rotation matrix $M \in \mathcal{M}$, and test example $x$:
\(\mathcal{L}[S](x) = \mathcal{L}[MS](Mx)\)
where $MS = {(Mx^{(i)}, y^{(i)})}_{i=1}^m$.</p>

<p><strong>Intuition:</strong> The algorithm’s predictions don’t change when we rotate the coordinate system.</p>

<p>Rotational invariance has showned to be the cause for ineffective feature selection in ML algorithms. The reason is that you need a lot more samples for the algo to first learn the rotation then perform variable selection. I highly encourage to read the paper <strong>Feature selection, L1 vs. L2 regularization and rotational invariance</strong> by the great <strong>Andrew Ng</strong>.</p>

<p><strong>Ridge</strong> constraint is $L2$ so it is a circle, symmetric in all directions (rotation invariant).
<strong>Lasso</strong> constraint is $L1$ so it is a diamond with corners pointing along the coordinate axis, so not rotationally invariant.</p>

<p>In a <strong>Sparse Environment:</strong></p>
<ul>
  <li>If the important features are correlated, you’re effectively in a <strong>Sparser</strong> environment, as you can combine the non-noise features into more important ones so <strong>Lasso</strong> outperforms.</li>
  <li>If the noise is correlated, you can reduce the noise dimensionality so you end up in a less-sparse environment, where Lasso suffers. In general, <strong>Lasso suffers when the true signal is non-sparse because it tends to overshrink small but important coefficients, adding some bias</strong>. Lasso loses more performance than <strong>Ridge</strong> when you go froma  sparse to a non-sparse environment. So you want the dimension reduction capability to be larger on important predictors than on noise.</li>
  <li>If you have general correlation (across both features and noise), <strong>ElasticNet</strong> will work best. It actually outperforms both Ridge and Lasso in most settings.</li>
</ul>

<p>Rotational Invariance and Sparsity are tightly related. If you have a highly sparse data and you apply a rotation, you’re mixing the important features with a lot of noise. This is why <strong>Ridge</strong> which is rotation invariant is worse than <strong>Lasso</strong> in high-sparsity regimes.</p>

<h3 id="the-grouping-effect">The Grouping Effect</h3>

<p>What is the grouping effect? It’s a property of ML algorithms to put similar weights to similar features. Simple no?</p>

<p><strong>The Grouping Effect</strong> (definition by Zou and Hastie, in <strong>Regularization and variable selection via the elastic net</strong> (2005)):: A regression method exhibits the <strong>grouping effect</strong> if the coefficients of highly correlated variables tend to be equal (up to a change of sign).</p>

<p>But why should we care about that?</p>
<ul>
  <li>In domains like economics, finance, or biology, related variables often move together and selecting them together makes the mode align with domain knowledge. For example, Genes in the same pathway should be selected togethe</li>
  <li>Correlated predictors <strong>jointly</strong> carry information about the response. Arbitrary exclusion may lose useful signal.</li>
  <li>Small perturbations in data shouldn’t drastically change which variable is selected. This is about <strong>stability in variable selection</strong></li>
  <li><strong>Interpretability:</strong> Identifies groups of important features, not arbitrary singletons. Lasso has indeed the tendency to “arbitrarily” choose one predictor amongst a group of highly correlated ones and set the rest to 0.</li>
</ul>

<p>Assume $x_i = x_j$ (identical predictors). Consider the penalized regression:</p>

\[\hat{\beta} = \arg\min_{\beta} \|y - X\beta\|^2 + \lambda J(\beta)\]

<p><strong>Strictly convex $J(\cdot)$:</strong> \(\hat{\beta}_i = \hat{\beta}_j, \quad \forall \lambda &gt; 0\)</p>

<p><strong>Lasso ($J(\beta) = |\beta|_1$):</strong> \(\hat{\beta}_i \hat{\beta}_j \geq 0\). But $\hat{\beta}_i$ may $\neq \hat{\beta}_j$</p>

<p>Only strict convexity, (which Lasso doesn’t have) guaranties grouping.</p>

<h3 id="why-elastic-net-wins">Why Elastic-Net wins</h3>

<p>I should probably start by dropping this Theorem, from <strong>Variable selection via nonconcave penalized likelihood and its oracle properties</strong>, from Jiangqing Fan and Runze Li (2001): In the $L_q$ penalty family (for \(q\geq 1\)), only the Lasso ($q = 1$) produces sparse solutions.</p>

<!-- ![Diagram](/images/lq_constraints.png) -->

<p align="center">
<img src="/images/lq_constraints.png" data-light-src="/images/lq_constraints.png" data-dark-src="/images/lq_constraints_dark.png" alt="Diagram" />
</p>

<p>This basically means that <strong>Bridge Regression</strong>, which is regression with regularization \(q \in (1,2]\), i.e. between Ridge and Lasso, does not have sharp corners. So it cannot perform Variable Selection.</p>

<p>So what do we want really out of our linear model?</p>

<ul>
  <li>Variable Selection</li>
  <li>Grouping Effect</li>
  <li>Non - Rotational Invariance</li>
</ul>

<p>Turns out that Elastic-Net satisfies all 3 of those criterias. It can perform Variable Selection by having those sharp corners in its constraint region. It also has the grouping effect, as the constraint is strictly convex. Finally, it is also non-rotationally invariant. So Elastic-Net FTW.</p>

<!-- <p align="center">
  <img src="/images/constraints_en.jpg" alt="Diagram" width="600">
</p> -->

<p align="center">
<img src="/images/constraints_en.jpg" data-light-src="/images/constraints_en.jpg" data-dark-src="/images/constraints_en_dark.png" alt="Diagram" width="600" />
</p>

<p>I still use Ridge, though.</p>

<hr />
<h2 id="references">References</h2>
<p><strong>The Elements ofStatistical Learning: Data Mining, Inference, and Prediction.</strong> (2009)<br />
Trevor Hastie, Robert Tibshirani, Jerome Friedman<br />
<a href="https://hastie.su.domains/ElemStatLearn/" target="_blank">Book</a></p>

<p><strong>Lecture notes on ridge regression</strong> (2023)<br />
Wessel N. van Wieringen<br />
<a href="https://arxiv.org/pdf/1509.09169" target="_blank">Notes</a></p>

<p><strong>Regularization and variable selection via the elastic net</strong> (2005)<br />
Hui Zou, Trevor Hastie<br />
<a href="https://hastie.su.domains/Papers/B67.2%20(2005)%20301-320%20Zou%20&amp;%20Hastie.pdf" target="_blank">Paper</a></p>

<p><strong>Variable selection via nonconcave penalized likelihood and its oracle properties</strong> (2001)<br />
Jianqing Fan, Runze Li<br />
<a href="https://fan.princeton.edu/sites/g/files/toruqf5476/files/documents/penlike.pdf" target="_blank">Paper</a></p>

<p><strong>Feature selection, L1 vs. L2 regularization, and rotational invariance</strong> (2004)<br />
Andrew Ng<br />
<a href="https://ai.stanford.edu/~ang/papers/icml04-l1l2.pdf" target="_blank">Paper</a></p>]]></content><author><name></name></author><summary type="html"><![CDATA[The Problem Shrinkage methods are used for two reasons: to deal with multicollinearity and do variable selection. When high multicollinearity is present, the design matrix $X^TX$ becomes degenerate. When perfect multicollinearity is present, $rank(X^TX) &lt; n$ and the matrix is singular and thus non-invertible, so it is impossible to solve OLS. As a motivating example, perform the eigendecomposition of the design matrix:]]></summary></entry><entry><title type="html">Ordinary Least Squares</title><link href="http://localhost:4000/blog/OLS/" rel="alternate" type="text/html" title="Ordinary Least Squares" /><published>2000-01-01T00:00:00+00:00</published><updated>2000-01-01T00:00:00+00:00</updated><id>http://localhost:4000/blog/OLS</id><content type="html" xml:base="http://localhost:4000/blog/OLS/"><![CDATA[\[\mathbf{Y = X\beta + \varepsilon}\]

<h2 id="assumptions-">Assumptions :</h2>

<p><strong>Linear Relationship:</strong> That goes without saying, but you implicitely assume that there is a linear relationship bewteen $\mathbf{X}$ and $\mathbf{Y}$.</p>

<p><strong>Strict Exogeneity:</strong> $\mathrm{E}[\boldsymbol{\varepsilon} \mid \mathbf{X}] = 0$</p>
<ul>
  <li><em>Consequences:</em> $\mathrm{E}[\boldsymbol{\varepsilon}] = 0$ and $Cov(X,\varepsilon) = \mathrm{E}[\mathbf{X}^T\boldsymbol{\varepsilon}] = 0$</li>
  <li>If it holds, regressors are exogenous. If violated, regressors are endogenous, OLS becomes biased, and instrumental variables may be needed.</li>
</ul>

<p><strong>No perfect multicollinearity</strong> $\Pr[\mathrm{rank}(\mathbf{X}) = p] = 1$</p>
<ul>
  <li>Regressors must be linearly independent</li>
  <li>If violated, $\boldsymbol{\beta}$ cannot be estimated, though prediction may still be possible.</li>
</ul>

<p><strong>Spherical Errors:</strong> $\mathrm{Var}[\boldsymbol{\varepsilon} \mid \mathbf{X}] = \sigma^2 \mathbf{I}_n$</p>
<ul>
  <li><em>Homoscedasticity:</em> $\mathrm{E}[\varepsilon_i^2 \mid \mathbf{X}] = \sigma^2$ for all $i$</li>
  <li><em>No Autocorrelation:</em> $\mathrm{E}[\varepsilon_i\varepsilon_j \mid \mathbf{X}] = 0$ for $i \neq j$</li>
  <li>If violated, OLS estimates are unbiased but inefficient; use GLS or robust estimation.</li>
</ul>

<p><strong>Normality:</strong> $\boldsymbol{\varepsilon} \mid \mathbf{X} \sim \mathcal{N}(\mathbf{0}, \sigma^2\mathbf{I}_n)$</p>

<h2 id="estimator-distribution-">Estimator Distribution :</h2>

\[\begin{align*}
\boldsymbol{\hat{\beta}} &amp;= (\mathbf{X}^T\mathbf{X})^{-1}\mathbf{X}^T\mathbf{y} \\
&amp;= (\mathbf{X}^T\mathbf{X})^{-1}\mathbf{X}^T(\mathbf{X}\boldsymbol{\beta} + \boldsymbol{\varepsilon}) \\
&amp;= (\mathbf{X}^T\mathbf{X})^{-1}\mathbf{X}^T\mathbf{X}\boldsymbol{\beta} + (\mathbf{X}^T\mathbf{X})^{-1}\mathbf{X}^T\boldsymbol{\varepsilon} \\
&amp;= \boldsymbol{\beta} + (\mathbf{X}^T\mathbf{X})^{-1}\mathbf{X}^T\boldsymbol{\varepsilon}
\end{align*}\]

<p><strong>Estimator Mean:</strong></p>

\[\begin{align*}
E[\boldsymbol{\hat{\beta}}] &amp;= E[\boldsymbol{\beta} + (\mathbf{X}^T\mathbf{X})^{-1}\mathbf{X}^T\boldsymbol{\varepsilon} | \mathbf{X}] \\
&amp;= \boldsymbol{\beta} + (\mathbf{X}^T\mathbf{X})^{-1}\mathbf{X}^T E[\boldsymbol{\varepsilon} | \mathbf{X}] \\
&amp;= \boldsymbol{\beta}
\end{align*}\]

<p><strong>Estimator Variance:</strong></p>

\[\begin{align*}
Var(\boldsymbol{\hat{\beta}}) &amp;= Var(\boldsymbol{\beta} + (\mathbf{X}^T\mathbf{X})^{-1}\mathbf{X}^T\boldsymbol{\varepsilon}) \\
&amp;= \boldsymbol{\beta} +Var((\mathbf{X}^T\mathbf{X})^{-1}\mathbf{X}^T\boldsymbol{\varepsilon})\\
&amp;= (\mathbf{X}^T\mathbf{X})^{-1}\mathbf{X}^T Var(\varepsilon) \mathbf{X}(\mathbf{X}^T\mathbf{X})^{-1} \\
&amp;= \sigma^2 (\mathbf{X}^T\mathbf{X})^{-1}
\end{align*}\]

<p>Under normality assumption $\boldsymbol{\varepsilon} \sim \mathcal{N}(\mathbf{0}, \sigma^2)$, $\qquad \qquad\boldsymbol{\hat{\beta}} \sim \mathcal{N}(\beta, \sigma^2 (\mathbf{X}^T\mathbf{X})^{-1})$.</p>

<h3 id="hypothesis-testing-">Hypothesis Testing :</h3>
<p>\(\text{RSS} = \varepsilon^\top \varepsilon = \sum_{i=1}^{n} (y_i - \hat{y}_i)^2\)</p>

\[\qquad \qquad \text{estimate variance } \sigma^2 \text{ by } \qquad \qquad 
\hat{\sigma}^2 = \frac{\text{RSS}}{n - p}
\quad \qquad \text{($n - p$ makes it unbiased } \mathbb{E}[\hat{\sigma}^2] = \sigma^2\text{)}\]

\[\text{RSE} = \sqrt{\sigma} = \sqrt{\frac{\varepsilon^\top \varepsilon}{n - p}}\]

<p><strong>Testing $\beta_j = 0$ :</strong>
Define the z-score \(z = \frac{\hat{\beta}}{SE(\hat{\beta})} = \frac{\hat{\beta}}{\hat{\sigma}\sqrt{(X^TX)}} \sim t_{n-p}\)</p>

<p>One can construct a confidence interval:</p>

\[\hat{\beta}_j \pm t_{n - p,\, 1 - \alpha/2} \; \text{SE}(\hat{\beta}_j)\]

\[\hat{\beta}_j \pm z_{1 - \alpha/2} \; \text{SE}(\hat{\beta}_j) \qquad \text{as $t_{n - p} \to \mathcal{N}(0,1)$ when $n \to \infty$}\]

<p><strong>Testing Model 1</strong> with $p_1$ parameters vs. <strong>Model 2</strong> with $p_2$ parameters, $p_1 &gt; p_2$:</p>

\[F = \frac{(RSS_0 - RSS_1) / (p_1-p_0)}{RSS_1/(n-p1)} \sim F_{p_1-p_0, n-p_1}\]

<h3 id="metrics">Metrics</h3>
<ul>
  <li>$\mathbf{R^2} = 1 - \frac{\text{RSS}}{\text{TSS}} \quad (= \rho_{xy}^2)$</li>
  <li><strong>Adjusted</strong> $\mathbf{R^2} = 1 - \left(\frac{1 - R^2}{n - p - 1}\right) \cdot (n - 1)$</li>
  <li><strong>AIC</strong> $= 2k - 2 \ln(L)$</li>
  <li><strong>BIC</strong> $= \ln(n)k - 2 \ln(L)$</li>
</ul>

<h3 id="gaussmarkov-theorem">Gauss–Markov Theorem</h3>

<p><strong>Theorem (Gauss–Markov):</strong>
If the following assumptions hold:</p>
<ul>
  <li>Linearity: $y = X\beta + \varepsilon$</li>
  <li>$\mathbb{E}[\varepsilon] = 0$</li>
  <li>$\operatorname{Var}(\varepsilon) = \sigma^2 I$</li>
  <li>$X$ has full column rank</li>
</ul>

<p>then the OLS estimator is <strong>Best Linear Unbiased Estimator (BLUE)</strong>.</p>

<p><strong>Proof:</strong></p>

<p>Let $\tilde{\beta} = C y$ be another linear estimator of $\beta$, with
$C = (X^\top X)^{-1} X^\top + D$</p>

\[\begin{aligned}
\mathbb{E}[\tilde{\beta}]
&amp;= \mathbb{E}[C y] \\
&amp;= \mathbb{E}\!\left[\big((X^\top X)^{-1} X^\top + D\big)(X\beta + \varepsilon)\right] \\
&amp;= \big((X^\top X)^{-1} X^\top + D\big) X \beta + \big((X^\top X)^{-1} X^\top + D\big)\mathbb{E}\! \left[\varepsilon\right] \\
&amp;= (X^\top X)^{-1} X^\top X \beta + D X \beta \\
&amp;= (I + D X)\beta.
\end{aligned}\]

<p>So $\tilde{\beta}$ is unbiased if and only if $D X = 0$.</p>

\[\begin{aligned}
\operatorname{Var}(\tilde{\beta})
&amp;= \operatorname{Var}(C y) \\
&amp;= C \operatorname{Var}(y) C^\top \\
&amp;= \sigma^2 C C^\top \\
&amp;= \sigma^2 \big( (X^\top X)^{-1} X^\top + D \big) \big( X (X^\top X)^{-1} + D^\top \big) \\
&amp;= \sigma^2 \left( (X^\top X)^{-1} + (X^\top X)^{-1} X^\top D^\top + D X (X^\top X)^{-1} + D D^\top \right).
\end{aligned}\]

<p>\(\operatorname{Var}(\tilde{\beta}) = \sigma^2 (X^\top X)^{-1} + \sigma^2 D D^\top.\)
as unbiasedness condition $D X = 0$</p>

\[\operatorname{Var}(\tilde{\beta}) \succeq \operatorname{Var}(\hat{\beta}) = \sigma^2 (X^\top X)^{-1}.\]

<p>Since $D D^\top$ is PSD, so  $\hat{\beta}$ has the smallest variance among all linear unbiased estimators and is therefore <strong>BLUE</strong>.</p>

<h3 id="frischwaughlovell-theorem">Frisch–Waugh–Lovell Theorem</h3>

<p>If you have an OLS:
\(Y = \beta_1X_1 + \beta_2X_2 + \varepsilon\)
Then $\beta_2$ will be the same as:
\(M_1Y = \beta_2M_1X_2 + M_1\varepsilon\)
where $M1 $be the <em>residual-maker</em> (or <em>annihilator</em>) matrix
which projects any vector onto the space orthogonal to the column space of $X_1$:
\(M_1 = I - X_1 (X_1^\top X_1)^{-1} X_1^\top\)</p>

<p><strong>Procedure:</strong></p>
<ul>
  <li><em>Residuals of $y$ on $X_1$:</em>
  $
  \tilde{y} = M_1 y = y - X_1 (X_1^\top X_1)^{-1} X_1^\top y.
  $</li>
  <li><em>Orthogonal component of $X_2$ wrt $X_1$:</em>
  $
  \tilde{X}_2 = M_1 X_2 = X_2 - X_1 (X_1^\top X_1)^{-1} X_1^\top X_2.
  $</li>
  <li><em>Regress $\tilde{y}$ on $\tilde{X}_2$:</em>
  $
  \tilde{\beta}_2 = (\tilde{X}_2^\top \tilde{X}_2)^{-1} \tilde{X}_2^\top \tilde{y}.
  $</li>
  <li>Then $\tilde{\beta}_2= \hat{\beta}_2$</li>
</ul>

<hr />
<h2 id="violated-assumptions-in-ols">Violated Assumptions in OLS</h2>

<h3 id="model-misspecification--functional-form">Model Misspecification / Functional Form</h3>
<p><strong>Issue:</strong> Wrong functional form, omitted variables, or nonlinearity. Violates $\mathbb{E}[\varepsilon|X] = 0$ if true relationship is nonlinear but model is linear.</p>

<p><strong>Consequences:</strong></p>
<ul>
  <li><strong>Biased Estimates:</strong> $\hat{\beta}$ biased and inconsistent when functional form is misspecified. Bias magnitude depends on degree of misspecification.</li>
  <li><strong>Omitted Variable Bias:</strong> If $y = \beta_0 + \beta_1 x_1 + \beta_2 x_2 + \varepsilon$ but only regress on $x_1$, then $\mathbb{E}[\hat{\beta}_1] = \beta_1 + \beta_2 \frac{\text{Cov}(x_1, x_2)}{\text{Var}(x_1)}$ (biased unless $x_1 \perp x_2$ or $\beta_2 = 0$).</li>
</ul>

<p><strong>Diagnostics:</strong></p>
<ul>
  <li><strong>Residual Plots:</strong> Plot residuals $\varepsilon$ vs. fitted values $\hat{y}$ or vs. individual predictors $x_j$. Should see no pattern (random scatter around zero). Systematic patterns (U-shape, curves, trends) indicate misspecification.</li>
  <li><strong>Partial Residual Plots:</strong> Plot $e + \hat{\beta}_j x_j$ vs. $x_j$ to detect nonlinearity in variable $x_j$ while controlling for others.</li>
</ul>

<p><strong>Remedies:</strong></p>
<ul>
  <li><strong>Nonlinear Transformations of Predictors:</strong> Use $\log(x)$, $\sqrt{x}$, $x^2$, $1/x$</li>
  <li><strong>Interaction Terms:</strong> Include $x_1 \cdot x_2$ if effect of $x_1$ depends on level of $x_2$.</li>
  <li><strong>Box-Cox Transformation:</strong> Transform dependent variable: $y^{(\lambda)} = \frac{y^\lambda - 1}{\lambda}$ (or $\log(y)$ if $\lambda = 0$). Choose $\lambda$ via MLE to improve model fit.</li>
</ul>

<h3 id="multicollinearity">Multicollinearity</h3>
<p><strong>Issue:</strong> High correlation among regressors causes an ill-conditioned $X^\top X$ matrix, leading to:</p>
<ul>
  <li><strong>Inflated Variance &amp; Unstable Estimates:</strong> The sampling variance, $\text{Var}(\hat{\beta}) = \sigma^2 (X^\top X)^{-1}$, becomes inflated. This instability in $\hat{\beta}$ leads to deflated t-statistics, increasing the risk of Type II error (failing to identify a significant effect).</li>
  <li><strong>Unreliable Interpretation:</strong> It becomes difficult to disentangle the individual effect of each predictor, making coefficient interpretation unreliable.</li>
</ul>

<p><strong>Diagnostics:</strong></p>
<ul>
  <li><strong>Variance Inflation Factor (VIF):</strong> $\text{VIF}_j = \frac{1}{1-R_j^2}$ for regressor $j$. A high VIF indicates multicollinearity, as $\text{Var}(\hat{\beta}_j) = \text{Var}(\hat{\beta}_j)^{\text{orth}} \times \text{VIF}_j$, where the variance is multiplied relative to the orthogonal case.</li>
  <li><strong>Condition Number:</strong> $\kappa = \sqrt{\lambda_\text{max} / \lambda_\text{min}}$ of $X$ or $X^\top X$. Values &gt; 30 suggest significant multicollinearity.</li>
</ul>

<p><strong>Remedies:</strong> Shrinkage (Ridge, Lasso), Dimensionality Reduction (Principal Component Regression), FWL Theorem.</p>

<h3 id="heteroskedasticity">Heteroskedasticity</h3>
<p><strong>Issue:</strong> Non-constant error variance, $\mathrm{Var}(\varepsilon_i) = \sigma_i^2$. OLS remains unbiased but is inefficient (no longer BLUE). The standard covariance matrix $\sigma^2(X^\top X)^{-1}$ is incorrect, leading to biased standard errors and invalid inference (t-statistics, confidence intervals).</p>

<p><strong>Diagnostics:</strong></p>
<ul>
  <li><strong>Graphics:</strong> Plot residuals ($e_i$) vs. predictors ($X_i$) or fitted values ($\hat{y}_i$) (should be around 0 with constant variance). Also plot squared residuals vs. predictors (need a flat line around $\sigma^2$).</li>
  <li><strong>tests :</strong> Breusch-Pagan (tests if variance depends on predictors), White (general test for heteroskedasticity), Goldfeld-Quandt tests for differing variance between two data subsets)</li>
</ul>

<p><strong>Remedies:</strong></p>
<ul>
  <li><strong>Robust Standard Errors (Eicker–Huber–White):</strong> The preferred solution for inference. Instead of the standard covariance matrix $\sigma^2(X^\top X)^{-1}$, it uses a consistent estimator:
      \(\widehat{V}_{ehw} = (X^\top X)^{-1} (X^\top \hat{\Omega} X) (X^\top X)^{-1}, \quad \hat{\Omega} = \text{diag}(\hat{\varepsilon}_1^2, \ldots, \hat{\varepsilon}_n^2)\)
      where \(\widehat{V}_{ehw}\) is the Eicker–Huber–White (EHW) robust covariance matrix. We can now do test using \(\hat{\beta} \overset{a}{\sim} \mathcal{N}(\beta, \widehat{V}_{ehw})\)</li>
  <li><strong>Weighted Least Squares (WLS):</strong> If $\sigma_i^2$ is known for each measurement, we can set $w_i = 1 / \sigma_i^2$</li>
  <li><strong>Transformations:</strong> Log, Box-Cox, etc., to stabilize variance (may also affect functional form).</li>
</ul>

<h3 id="autocorrelated-errors">Autocorrelated Errors</h3>
<p><strong>Issue:</strong> Non-zero covariance between errors, $\mathrm{Cov}(\varepsilon_i, \varepsilon_j) \neq 0$ for $i \neq j$. This violates the Gauss-Markov assumption that errors are independent. In time series data, this commonly follows an AR(1) process: $\varepsilon_t = \rho \varepsilon_{t-1} + u_t$ where $u_t \sim \text{IID}(0, \sigma_u^2)$.</p>

<p><strong>Consequences:</strong></p>
<ul>
  <li><strong>Reduced Effective Sample Size:</strong> With positive autocorrelation ($\rho &gt; 0$), consecutive observations are not fully independent. For AR(1) errors, the variance inflation factor is approximately $\frac{1+\rho}{1-\rho}$, so \(\text{Var}(\hat{\beta})_{AR(1)} \approx \frac{1+\rho}{1-\rho} \cdot \text{Var}(\hat{\beta})_{IID}\).</li>
  <li><strong>Biased Inference:</strong> OLS estimators remain unbiased and consistent, but are inefficient. The conventional OLS standard errors are inconsistent:</li>
  <li>For $\rho &gt; 0$: Standard errors are typically <em>underestimated</em>, making confidence intervals artificially narrow and inflating t-statistics (increased Type I error rates).</li>
  <li>For $\rho &lt; 0$: Standard errors are typically <em>overestimated</em>, leading to conservative inference (increased Type II error rates).</li>
</ul>

<p><strong>Diagnostics:</strong></p>
<ul>
  <li><strong>Residual Plots:</strong> Plot residuals against time; look for tracking patterns where adjacent residuals tend to have similar signs and magnitudes. ACF / PACF plots also.</li>
  <li><strong>Durbin-Watson Test:</strong> 
      \(d = \frac{\sum_{t=2}^T (e_t - e_{t-1})^2}{\sum_{t=1}^T e_t^2} \approx 2(1-\hat{\rho})\)
      Tests $H_0: \rho = 0$ vs $H_1: \rho \neq 0$ (or one-sided alternatives). Values significantly below 2 suggest positive autocorrelation; values significantly above 2 suggest negative autocorrelation. Limitation: Only tests for AR(1) autocorrelation and is inconclusive near 2.</li>
  <li><strong>Box-Pierce Test:</strong> General test for autocorrelation up to lag $h$:
      \(Q_{BP} = n\sum_{k=1}^h \hat{\rho}_k^2 \sim \chi^2_h\)
      where $\hat{\rho}_k$ is the sample autocorrelation at lag $k$.</li>
  <li><strong>Ljung-Box Test:</strong> Modified version with better small-sample properties:
      \(Q_{LB} = n(n+2)\sum_{k=1}^h \frac{\hat{\rho}_k^2}{n-k} \sim \chi^2_h\)
      Generally preferred over Box-Pierce in practice.</li>
</ul>

<p><strong>Remedies:</strong></p>
<ul>
  <li><strong>Generalized Least Squares (GLS):</strong> For AR(1) errors, transform data via $y_t^* = y_t - \rho y_{t-1}$ and $X_t^* = X_t - \rho X_{t-1}$. The <strong>Cochrane-Orcutt</strong> procedure iteratively estimates $\rho$ from OLS residuals and applies the transformation until convergence.</li>
  <li><strong>Newey-West HAC Standard Errors:</strong> Heteroskedasticity and Autocorrelation Consistent (HAC) robust covariance estimator: 
      \(\widehat{V}_{NW} = (X^\top X)^{-1} \hat{\Omega} (X^\top X)^{-1}\)
      where $\hat{\Omega}$ accounts for autocorrelation up to lag $L$ using a kernel weighting scheme.</li>
  <li><strong>Include Lagged Dependent Variable:</strong> For time series models, adding $y_{t-1}$ as a regressor may capture autocorrelation in the original error term, transforming it into proper model dynamics. Note: This creates a dynamic panel model with different asymptotic properties.</li>
</ul>

<h3 id="heavy-tailed--non-normal-errors">Heavy-tailed / Non-normal Errors</h3>
<p><strong>Issue:</strong> Errors $\varepsilon_i$ deviate from normality, particularly with heavy tails (high kurtosis). Normality not required for OLS unbiasedness, but departures affect inference and efficiency.<br />
<strong>Consequences:</strong></p>
<ul>
  <li><strong>Variance Overestimation:</strong> $\hat{\sigma}^2$ highly sensitive to extreme values, typically <em>overestimated</em> with heavy-tailed outliers. This inflates standard errors, making CIs artificially wide and $\hat{\beta}$ appear less significant (reduces power, increases Type II error).</li>
  <li><strong>Inference Validity:</strong> t-tests and F-tests assume normality for exact finite-sample validity. Under non-normality, these rely on CLT for asymptotic approximations. With heavy tails (Cauchy, Pareto with $\alpha &lt; 2$), CLT fails and inference is invalid even asymptotically.</li>
</ul>

<p><strong>Diagnostics:</strong></p>
<ul>
  <li><strong>Q-Q Plot:</strong> Sample quantiles vs. theoretical normal quantiles. Heavy tails appear as deviations at extremes (both ends curving away).</li>
  <li><strong>Sample Kurtosis:</strong> Excess kurtosis $&gt; 0$ indicates heavier tails. Kurtosis $&gt; 10$ suggests severe heavy-tailedness.</li>
  <li><strong>Shapiro-Wilk:</strong> Most powerful normality test for $n &lt; 2000$. $W = \frac{(\sum_{i=1}^n a_i x_{(i)})^2}{\sum_{i=1}^n (x_i - \bar{x})^2}$. P-value $&lt; \alpha$ rejects normality.</li>
  <li><strong>Goodness of fit tests:</strong> Kolmogorov-Smirnov (compares empirical CDF to theoretical normal CDF), Anderson-Darling (similar to KS but more weight on the tails)</li>
</ul>

<p><strong>Remedies:</strong></p>
<ul>
  <li><strong>Robust SEs:</strong> HC or HAC standard errors remain valid under non-normality (rely on asymptotic approximations).</li>
  <li><strong>Bootstrap SEs:</strong> Non-parametric bootstrap provides valid inference without distributional assumptions. Use residual bootstrap or pairs bootstrap.</li>
  <li><strong>Studentized Residuals:</strong> $r_i = \frac{\varepsilon_i}{\hat{\sigma}\sqrt{1-H_{ii}}}$ with $H = X(X^TX)^{-1}X^T$.</li>
</ul>

<hr />
<h2 id="references">References</h2>
<p><strong>The Elements ofStatistical Learning: Data Mining, Inference, and Prediction.</strong> (2009)<br />
Trevor Hastie, Robert Tibshirani, Jerome Friedman<br />
<a href="https://hastie.su.domains/ElemStatLearn/" target="_blank">Book</a></p>

<p><strong>The Truth about Linear Regression</strong> (2015)<br />
Cosma Rohilla Shalizi<br />
<a href="https://www.stat.cmu.edu/~cshalizi/TALR/TALR.pdf" target="_blank">Book</a></p>

<p><strong>Linear Model and Extensions</strong> (2024)<br />
Pend Ding<br />
<a href="https://arxiv.org/pdf/2401.00649" target="_blank">Book</a></p>]]></content><author><name></name></author><summary type="html"><![CDATA[\[\mathbf{Y = X\beta + \varepsilon}\]]]></summary></entry></feed>